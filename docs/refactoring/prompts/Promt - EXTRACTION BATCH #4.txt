РОЛЬ: Ты выполняешь механический, атомарный перенос кода в проекте Supervertaler.
Режим: ACTIVE REFACTOR, но с обязательными pre-flight проверками (как в Batch
#1/#2/#3a/#3b/#3c). Если любая pre-flight проверка провалится — ОСТАНОВИСЬ и
сообщи о несоответствии, не пытайся "исправить попутно".

ПРЕДПОСЫЛКА: этот батч запускается только после подтверждённого GO по Batch
#3c (последний под-батч Step 3 EXTRACTION_PLAN.md). Это первый батч Step 4 —
первый шаг плана, который НЕ является под-батчем Step 3. Обновление
VALIDATION_BACKLOG.md (закрытие 7 пунктов по Batch #3b/#3c владельцем,
2026-09-14) коммитится ОТДЕЛЬНЫМ коммитом ДО начала этого батча — не включать
этот файл в diff Batch #4.

Унаследовано из Batch #2/#3c: если pre-flight найдёт, что что-либо в этом
скоупе совпадает по названию/назначению с уже существующим кодом в modules/
— не верить этому на слово, сравнить поведение, и при любом расхождении
ОСТАНОВИСЬ, не выбирать "правильную" версию сам.

СКОУП BATCH #4 (и ничего больше):
Перенести 3 класса чистых QThread-воркеров из Supervertaler.py в НОВЫЙ пакет
modules/workers/ (по одному классу на файл, как в modules/dialogs/):
- TMSearchWorker       → modules/workers/tm_search.py   (монолит: 5334–5457)
- ProofreadWorker      → modules/workers/proofread.py   (монолит: 6169–6326)
- GlossaryExtractionWorker → modules/workers/glossary.py (монолит: 6337–6408)

НЕ ПЕРЕНОСИТСЯ: PreTranslationWorker (монолит: 5460–6166) — физически лежит
МЕЖДУ TMSearchWorker и ProofreadWorker. Требует отдельного планирования
декомпозиции из-за зависимостей от parent_app (см. Step 13 EXTRACTION_PLAN.md).
В этом батче это не просто "вне скоупа" — это класс, зажатый ровно между
двумя переносимыми, поэтому есть реальный риск задеть его границы при
удалении соседей. Обращаться с этим как с отдельным жёстким требованием, не
только формальным исключением из списка.

ЖЁСТКИЕ ЗАПРЕТЫ:
- Не переносить, не изменять, не переформатировать PreTranslationWorker —
  ни единой строки, ни окружающих его пустых строк/комментариев.
- Не переименовывать 3 переносимых класса, не менять сигнатуры конструкторов.
- ProofreadWorker.cancel и TMSearchWorker.cancel по плану идентичны текстово
  — это НЕ повод выносить общий базовый класс, миксин или delegate. Оставить
  оба тела дословно раздельными, по классу, как сейчас. Это решение владельца,
  зафиксированное в EXTRACTION_PLAN.md §Step 4, не гейт для пересмотра.
- Не трогать DYNAMIC_ENTRY_POINT методы.
- Не удалять и не помечать dead-код.
- Не создавать единый modules/workers.py или modules/workers/worker.py на все
  три класса — строго по файлу на класс, плюс __init__.py с реэкспортом (по
  образцу modules/dialogs/__init__.py).
- Один шаг = один коммит. VALIDATION_BACKLOG.md в этот коммит не включать.

═══════════════════════════════════════════════════════════════
ЭТАП 0 — БАЗОВАЯ ЛИНИЯ
═══════════════════════════════════════════════════════════════
1. Убедиться, что рабочая копия чистая (git status), продолжить от коммита
   Batch #3c (и, если применимо, от отдельного коммита апдейта
   VALIDATION_BACKLOG.md — см. ПРЕДПОСЫЛКУ).
2. SHA256 всех .py файлов → manifest_before_batch4.txt.
3. py_compile всех .py файлов — чисто.
4. Подсчитать baseline_counts_batch4.txt: те же 8 метрик — .connect(,
   QShortcut(, create_shortcut(, QTimer(, timeout.connect, QTimer.timeout,
   singleShot, QMetaObject.invokeMethod.

═══════════════════════════════════════════════════════════════
ЭТАП 1 — PRE-FLIGHT (не начинать перенос, пока весь этап не пройден)
═══════════════════════════════════════════════════════════════

1.1 ГРАНИЦЫ И СОСТАВ (AST, не доверять дословно числам из отчётов)
- Найти реальные границы всех 3 классов через AST (не текстовый grep по
  номерам строк из EXTRACTION_PLAN.md): TMSearchWorker, ProofreadWorker,
  GlossaryExtractionWorker.
- ОБЯЗАТЕЛЬНО тем же способом определить точные границы PreTranslationWorker
  и подтвердить, что между end_lineno TMSearchWorker и lineno
  PreTranslationWorker, а также между end_lineno PreTranslationWorker и
  lineno ProofreadWorker нет ничего, что логически принадлежит одному из
  переносимых классов (декораторы, class-level комментарии над классом,
  case-специфичные пустые строки, если они значимы) — то есть, что границы
  реально дискретны, а не "заходят" друг в друга.
- Если фактические диапазоны (5334–5457 / 6169–6326 / 6337–6408) отличаются
  от заявленных, или PreTranslationWorker найден не там, где ожидалось —
  сообщить и уточнить скоуп ПЕРЕД переносом, не решать самостоятельно.

1.2 ЧИСТОТА КЛАССОВ / ИНТЕРФЕЙС
- Для каждого из 3 классов: точная сигнатура __init__ (только примитивы +
  pyqtSignal, без self.main_window/self.parent_app/иных ссылок на MainWindow).
- Явно подтвердить фактически (не по документации), что ни один из 3 классов
  не обращается к parent_app — это именно то, что отличает их от
  PreTranslationWorker и оправдывает перенос в этом батче, а не позже.

1.3 СОДЕРЖИМОЕ modules/workers/ — ПОЛНАЯ ИНВЕНТАРИЗАЦИЯ
- Подтвердить, что пакета modules/workers/ не существует. Если существует —
  прочитать целиком, что там уже есть, прежде чем что-либо добавлять.

1.4 КРИТИЧЕСКИЙ ГЕЙТ — ПОВЕДЕНЧЕСКОЕ СРАВНЕНИЕ "ДУБЛЕЙ"
Обязательный шаг, даже если ожидается, что он будет пустым (переносим в новый
пакет, не сливаем с существующим).
- Grep/просмотр modules/*.py на классы/функции с похожим именем или
  назначением (QThread-воркеры для TM-поиска / проверки / извлечения
  терминов) — обратить внимание на modules/tm_manager_qt.py и другие файлы,
  уже использующие QThread (см. предыдущие отчёты), — это НЕ обязательно
  дубликаты, но проверить фактически, не предполагать по названию.
- Если что-то по-настоящему пересекающееся найдено — сравнить поведение
  (3-5 сценариев), при расхождении ОСТАНОВИТЬСЯ и показать конкретный пример,
  ждать решения владельца.
- ОТДЕЛЬНО (это не гейт слияния, а гейт подтверждения намеренного
  НЕ-слияния): зафиксировать явно, что тела TMSearchWorker.cancel и
  ProofreadWorker.cancel текстово идентичны (или чем отличаются, если
  отличаются) — и что оба переносятся как раздельные методы в раздельных
  классах без исключений, согласно ЖЁСТКОМУ ЗАПРЕТУ выше.
- Если ничего постороннего не найдено — зафиксировать явно: "дублей вне
  батча не найдено, гейт формально пройден" — и продолжать к Этапу 2.

1.5 CALLSITES И CALLERS
- Grep по всему репозиторию (Supervertaler.py + modules/*.py) на прямые
  конструкторы TMSearchWorker(...), ProofreadWorker(...),
  GlossaryExtractionWorker(...), плюс все .start()/.cancel()/сигнальные
  .connect(...) на экземпляры этих трёх. Полный список →
  callsites_before_batch4.txt.
- Ориентир по документированным местам (после Batch #3c, могли сместиться):
  GlossaryExtractionWorker ~19152/19159, ProofreadWorker ~48517/48601,
  TMSearchWorker ~63579/63594 — сверить фактически через grep, не доверять
  числам дословно.
- Подтвердить, что все три класса не вызываются как self.ИмяКласса(...)
  нигде помимо ожидаемых мест создания воркера (обычная практика для этого
  паттерна — воркер создаётся один раз на операцию, не хранится как
  постоянный атрибут).

1.6 ПРОВЕРКА НА DYNAMIC_ENTRY_POINT И СТРОКОВЫЕ ССЫЛКИ
- Проверить каждое из 3 имён на getattr/hasattr/setattr,
  QMetaObject.invokeMethod, DYNAMIC_ENTRY_POINT список (DEAD_CODE_REPORT.md).

1.7 ЛЕНИВЫЕ ИМПОРТЫ ВНУТРИ КЛАССОВ
- Проверить локальные import-выражения внутри тел 3 классов, перенести как
  есть, без изменений.

1.8 СНИМОК PreTranslationWorker (специфично для этого батча)
- До каких-либо изменений сохранить точное байтовое содержимое блока
  PreTranslationWorker (по его AST-границам из 1.1) в
  pretranslation_worker_before_batch4.txt вместе с SHA256. Это будет
  контрольная точка для Этапа 3.

═══════════════════════════════════════════════════════════════
ЭТАП 2 — ПЕРЕНОС (только после чистого прохождения Этапа 1.4)
═══════════════════════════════════════════════════════════════
1. Создать modules/workers/__init__.py, modules/workers/tm_search.py,
   modules/workers/proofread.py, modules/workers/glossary.py.
2. Перенести тела 3 классов ВЕРБАТИМ, по одному классу на файл (без
   изменений форматирования, без "улучшений").
3. modules/workers/__init__.py — реэкспорт по образцу
   modules/dialogs/__init__.py:
   from .tm_search import TMSearchWorker
   from .proofread import ProofreadWorker
   from .glossary import GlossaryExtractionWorker
   __all__ = ["TMSearchWorker", "ProofreadWorker", "GlossaryExtractionWorker"]
4. В Supervertaler.py добавить импорт сразу после существующего блока
   импортов modules.* (после строки импорта modules.dialogs):
   from modules.workers import (TMSearchWorker, ProofreadWorker,
   GlossaryExtractionWorker)  # QThread-воркеры (Batch #4)
5. Удалить оригинальные определения 3 классов из Supervertaler.py, оставив
   PreTranslationWorker строго на месте, без единого изменения вокруг него.
6. Немедленно после удаления заново снять AST-границы PreTranslationWorker в
   изменённом файле и сравнить байт-в-байт с
   pretranslation_worker_before_batch4.txt (см. 1.8). Должны совпасть
   полностью.
7. Убедиться, что все конструкторы/.start()/.cancel()/.connect(...) на 3
   класса в остальном коде монолита остались текстуально идентичными —
   меняется только источник импорта.

═══════════════════════════════════════════════════════════════
ЭТАП 3 — ВАЛИДАЦИЯ (обязательна, отчитаться по каждому пункту)
═══════════════════════════════════════════════════════════════
1. python -m py_compile на Supervertaler.py и всех 4 новых файлах — чисто.
2. Повторить подсчёт 8 метрик — сравнить с baseline_counts_batch4.txt, числа
   ДОЛЖНЫ совпасть точно.
3. Повторить grep callsites — сравнить с callsites_before_batch4.txt, набор
   мест вызова идентичен по содержимому (номера строк могут сместиться из-за
   удаления кода выше по файлу — это ожидаемо и не является расхождением;
   сверять по самому вызову, не по номеру строки).
4. SHA256 всех файлов, кроме Supervertaler.py и 4 новых файлов
   modules/workers/*, должны совпасть с manifest_before_batch4.txt.
5. PreTranslationWorker BYTE-IDENTITY CHECK (см. 1.8/2.6) — явное
   подтверждение с SHA256 до/после. Это отдельный обязательный пункт отчёта,
   не часть общего SHA256-diff (см. п.4), потому что сам Supervertaler.py
   меняется, но его содержимое ВНУТРИ границ PreTranslationWorker — нет.
6. ФУНКЦИОНАЛЬНЫЕ СЦЕНАРИИ (прогнать все, пункт за пунктом):
   - TM-поиск из SuperLookup и/или из грида сегментов (TMSearchWorker) —
     включая отмену поиска на середине (cancel), без краша.
   - Proofread-прогон на нескольких сегментах (ProofreadWorker) — включая
     отмену на середине (cancel), без краша.
   - Извлечение терминов из termbase-вкладки (GlossaryExtractionWorker) —
     термины извлекаются корректно.
   - Хотя бы один сценарий пакетного перевода через PreTranslationWorker —
     он не был перенесён, но физически лежал между двумя перенесёнными
     классами, поэтому явно исключить случайное повреждение по соседству.
7. Обычный smoke-тест (как в предыдущих батчах): создание проекта → импорт →
   правка сегмента → подтверждение → сохранение → перезапуск → загрузка →
   проверка состояния.

═══════════════════════════════════════════════════════════════
ЭТАП 4 — ОТЧЁТ (обязательный формат, не просто "готово")
═══════════════════════════════════════════════════════════════
- Результат Этапа 1.4: явное подтверждение "дублей вне батча не найдено" ЛИБО
  результат сравнения, если что-то найдено; плюс отдельное явное
  подтверждение по TMSearchWorker.cancel/ProofreadWorker.cancel (идентичны
  текстово, оставлены раздельно по решению владельца — не объединялись).
- Diff stat (git diff --stat).
- Callsites diff (before/after) с объяснением ожидаемых изменений (включая
  ожидаемый сдвиг номеров строк).
- Counts diff — все 8 метрик.
- SHA256 diff — какие файлы изменились (ожидается 5: Supervertaler.py + 4
  новых файла в modules/workers/).
- PreTranslationWorker byte-identity check — явный SHA256 до/после, PASS/FAIL.
- Чеклист функциональных сценариев (Этап 3.6) — пункт за пунктом,
  пройдено/не пройдено.
- Результат обычного smoke-теста.
- "Непокрытые проверки" — если что-то не удалось прогнать end-to-end
  (типично: нужен живой API-ключ для полного TM/proofread/glossary прогона
  на реальных данных), явный список: что / почему / риск / как проверить
  вручную — для внесения в VALIDATION_BACKLOG.md. Если таких пунктов нет —
  явно написать "нет".
- Явный GO/NO-GO с обоснованием.
- Один git-коммит: "Extract QThread workers into modules/workers/ (Batch #4,
  Step 4 of EXTRACTION_PLAN.md)".

СТОП-УСЛОВИЯ:
1. Если Этап 1.1 находит, что фактические границы отличаются от заявленных,
   или что PreTranslationWorker не изолирован чисто между двумя переносимыми
   классами — остановиться, уточнить скоуп у владельца.
2. Если Этап 1.4 находит поведенческое расхождение с чем-то реально похожим
   в modules/ (не внутрибатчевый случай cancel/cancel, который уже решён
   владельцем) — остановиться немедленно, показать конкретный пример, ждать
   решения владельца.
3. Если PreTranslationWorker byte-identity check (Этап 3.5) — FAIL:
   остановиться немедленно, откатить (git revert), не пытаться
   "восстановить" или "починить" самостоятельно.
4. После Этапа 4 — остановиться и ждать подтверждения владельца перед тем,
   как переходить к Step 5 (Undo-менеджер, будущий Batch #5). Не продолжать
   автоматически.
