РОЛЬ: Ты выполняешь механический перенос кода в проекте Supervertaler,
с обязательным поведенческим сравнением ДО переноса. Режим: ACTIVE REFACTOR
с жёсткими стоп-условиями. Если pre-flight находит поведенческое расхождение
между дублирующимися реализациями — ОСТАНОВИСЬ и сообщи, не выбирай
"правильную" версию сам.

ПРЕДПОСЫЛКА: этот батч запускается только после подтверждённого GO по
Batch #3b (диалоги → modules/dialogs/). Это последний из трёх под-батчей
Step 3 EXTRACTION_PLAN.md и самый рискованный из трёх, несмотря на
формальную метку LOW в плане — единственный, где происходит СЛИЯНИЕ с уже
существующим живым модулем, как в Batch #2 с tag_manager.py, где
документация тоже ошибочно называла код "дублем".

СКОУП BATCH #3c (и ничего больше):
Перенести в существующий modules/styled_widgets.py 4 класса из
Supervertaler.py (диапазон строк 72559–72869):
PinkCheckmarkCheckBox, BlueCheckmarkCheckBox, OrangeCheckmarkCheckBox,
CustomRadioButton.

РЕШЕНИЕ ПО АРХИТЕКТУРЕ (уже принято владельцем, не пересматривать):
Расширяем существующий modules/styled_widgets.py. НЕ создаём отдельный
modules/checkboxes.py. Если гейт 1.4 подтвердит, что поведение монолитных
классов идентично существующим в styled_widgets.py с точностью до цвета —
интегрировать через параметризацию (базовый класс + цветовой параметр или
class-атрибут), а не хранить несколько копий тела paintEvent. Имена
PinkCheckmarkCheckBox/BlueCheckmarkCheckBox/OrangeCheckmarkCheckBox/
CustomRadioButton должны продолжать существовать и работать с той же
сигнатурой конструктора — это касается способа реализации внутри
styled_widgets.py, а не отказа от публичных имён.

ЖЁСТКИЕ ЗАПРЕТЫ:
- Не переименовывать классы, не менять сигнатуру конструктора ни одного
  из 4-х.
- Не трогать DYNAMIC_ENTRY_POINT методы.
- Не удалять и не помечать dead-код.
- Не "улучшать" попутно уже существующие CheckmarkCheckBox/
  CheckmarkRadioButton/Purple/Teal-варианты в styled_widgets.py — только
  точечные изменения, необходимые для параметризации.
- Не унифицировать самостоятельно, если гейт 1.4 найдёт расхождение —
  это решение владельца, не механического рефакторинга.
- Один шаг = один коммит.

РАЗРЕШЕНИЕ (формализовано после Batch #2 и #3b): если на этапе 1.1 найдены
устаревшие диапазоны строк/факты в CODE_MAP_REFACTOR.md или EXTRACTION_PLAN.md
— можно поправить их прямо в этом же коммите, при условии явного отдельного
пункта в отчёте Этапа 4 ("что и почему изменено в документации"). Это не
нарушает "один шаг = один коммит".

ТРЕБОВАНИЕ К ОТЧЁТУ: если Этап 3 не смог прогнать что-то end-to-end
(типично — нужен живой API-ключ/железо/сеть, недоступные в headless-среде),
это ОБЯЗАТЕЛЬНО должно попасть в отчёт Этапа 4 отдельным разделом
"Непокрытые проверки": что / почему не прогнано / риск / как проверить
руками. Эти пункты затем переносятся в VALIDATION_BACKLOG.md (корень
репозитория, если файла нет — создать по аналогии с уже заведёнными
записями по Batch #3b).

═══════════════════════════════════════════════════════════════
ЭТАП 0 — БАЗОВАЯ ЛИНИЯ
═══════════════════════════════════════════════════════════════
1. Убедиться, что рабочая копия чистая (git status), продолжить от коммита
   Batch #3b.
2. SHA256 всех .py файлов → manifest_before_batch3c.txt.
3. py_compile всех .py файлов — чисто.
4. Подсчитать baseline_counts_batch3c.txt: те же 8 метрик — .connect(,
   QShortcut(, create_shortcut(, QTimer(, timeout.connect, QTimer.timeout,
   singleShot, QMetaObject.invokeMethod.

═══════════════════════════════════════════════════════════════
ЭТАП 1 — PRE-FLIGHT (не начинать перенос, пока весь этап не пройден)
═══════════════════════════════════════════════════════════════

1.1 ГРАНИЦЫ И СОСТАВ (AST, не доверять дословно числам из отчётов)
- Найти реальные границы всех 4 классов в диапазоне 72559–72869 через AST.
- Если фактический список/границы отличаются от заявленного — сообщить и
  уточнить скоуп ПЕРЕД переносом.

1.2 ЧИСТОТА КЛАССОВ
- Для каждого из 4 классов: обращается ли конструктор/paintEvent к
  self.theme_manager, self.main_window или иному состоянию MainWindow?
  Ожидается, что это чистые Qt-виджеты без MainWindow-связности —
  подтвердить фактически, не полагаться на ожидание.

1.3 СОДЕРЖИМОЕ modules/styled_widgets.py — ПОЛНАЯ ИНВЕНТАРИЗАЦИЯ
- Прочитать modules/styled_widgets.py целиком. Составить точный список всех
  классов, которые там уже есть, включая базовый CheckmarkCheckBox и его
  цветовые варианты (Purple/Teal — по DEPENDENCY_MAP.md) и
  CheckmarkRadioButton.
- Для каждого из 4 монолитных классов зафиксировать: с каким классом
  styled_widgets.py он потенциально пересекается по названию/назначению.

1.4 КРИТИЧЕСКИЙ ГЕЙТ — ПОВЕДЕНЧЕСКОЕ СРАВНЕНИЕ ДУБЛЕЙ
Это обязательный шаг. Не переходить к Этапу 2, пока он не завершён.
CODE_MAP_REFACTOR.md §10.1–§10.2 утверждает, что paintEvent трёх монолитных
чекбоксов — "копия" styled_widgets.CheckmarkCheckBox, а CustomRadioButton
дублирует виджет оттуда же. Это утверждение того же рода, что оказалось
частично неверным для tag_manager в Batch #2 (одинаковые имена методов при
разном поведении) — проверять конкретно, не доверять на слово.
- Привести полный текст paintEvent (и __init__, если переопределён) для
  всех 3 монолитных чекбоксов (Pink/Blue/Orange) и для базового
  CheckmarkCheckBox из styled_widgets.py — сравнить построчно/AST-
  нормализованно.
- Определить, где именно у монолитных версий "зашит" цвет: константа в
  теле метода? class-атрибут? аргумент конструктора? От этого зависит
  способ параметризации на Этапе 2.
- Сравнить CustomRadioButton (монолит) с CheckmarkRadioButton
  (styled_widgets) — это РАЗНЫЕ ИМЕНА. В первую очередь проверить, один ли
  это виджет по смыслу и поведению (прочитать оба тела полностью), а не
  считать их автоматически парой только по функциональной категории
  "радиокнопка".
- Подготовить 3-5 тестовых сценариев на каждую пару (checked/unchecked,
  hover, disabled, разные размеры шрифта UI, если применимо) — создать
  оба варианта виджета в минимальном offscreen-харнессе PyQt6 и сравнить
  рендер (пиксельно, если возможно) либо как минимум точно описать
  визуальные параметры (размер, hex-цвета, форма чекмарка/индикатора,
  поведение в disabled).
- РЕЗУЛЬТАТ СРАВНЕНИЯ:
  * Если поведение идентично с точностью до цвета для всех 4 пар —
    зафиксировать явно ("поведение идентично по цвету, подтверждено N
    сценариями на пару") и продолжать к Этапу 2 с параметризацией.
  * Если найдено ЛЮБОЕ расхождение хотя бы в одном сценарии хотя бы для
    одной пары (включая случай, когда CustomRadioButton и
    CheckmarkRadioButton окажутся разными по смыслу виджетами) —
    ОСТАНОВИТЬСЯ. Показать оба варианта на конкретном сценарии, где они
    разошлись, и ждать решения владельца. Не решать самостоятельно,
    объединять их или оставлять раздельно.

1.5 CALLSITES И CALLERS
- Grep по всему репозиторию (Supervertaler.py + modules/*.py) на конструкторы
  PinkCheckmarkCheckBox(...), BlueCheckmarkCheckBox(...),
  OrangeCheckmarkCheckBox(...), CustomRadioButton(...). Полный список →
  callsites_before_batch3c.txt. Зафиксировать, на каких вкладках/диалогах
  они используются (ожидаемо: TM, Termbase, AI-настройки — подтвердить
  фактически, не предполагать).
- РЕШЕНИЕ ПО УМОЛЧАНИЮ (можно отклониться с обоснованием, если 1.4
  потребует иного): сохранить вызовы конструкторов в монолите текстуально
  идентичными (PinkCheckmarkCheckBox(...) продолжает работать так же),
  только источник импорта меняется на modules.styled_widgets. Указать в
  отчёте, сколько call sites это затрагивает.

1.6 ПРОВЕРКА НА DYNAMIC_ENTRY_POINT И СТРОКОВЫЕ ССЫЛКИ
- Проверить все 4 имени на getattr/hasattr/setattr, QMetaObject.invokeMethod,
  DYNAMIC_ENTRY_POINT список.

1.7 ЛЕНИВЫЕ ИМПОРТЫ ВНУТРИ КЛАССОВ
- Проверить локальные import-выражения внутри тел 4 классов, перенести как
  есть.

═══════════════════════════════════════════════════════════════
ЭТАП 2 — ПЕРЕНОС (только после чистого прохождения Этапа 1.4)
═══════════════════════════════════════════════════════════════
1. Если гейт 1.4 подтвердил идентичность-по-цвету для Pink/Blue/Orange:
   реализовать их в styled_widgets.py как тонкие подклассы существующего
   CheckmarkCheckBox, параметризованные найденным на 1.4 способом (цветовая
   константа/атрибут), НЕ дублируя тело paintEvent три раза.
2. Если гейт 1.4 подтвердил, что CustomRadioButton и CheckmarkRadioButton —
   один и тот же виджет по поведению: оставить одну каноническую реализацию
   (уже существующую CheckmarkRadioButton) и сделать CustomRadioButton
   алиасом/тонким подклассом для сохранения имени в call sites. Если это НЕ
   подтвердилось (наиболее вероятный по имени сценарий) — перенести
   CustomRadioButton ВЕРБАТИМ как отдельный класс, без слияния с
   CheckmarkRadioButton.
3. Добавить в Supervertaler.py импорт всех 4 имён из modules.styled_widgets.
4. Удалить оригинальные определения из Supervertaler.py.
5. Убедиться, что все конструкторы (PinkCheckmarkCheckBox(...) и т.д.)
   в остальном коде монолита остались текстуально идентичными.

═══════════════════════════════════════════════════════════════
ЭТАП 3 — ВАЛИДАЦИЯ
═══════════════════════════════════════════════════════════════
1. py_compile всех файлов — чисто.
2. Сравнить baseline_counts_batch3c.txt с counts_after_batch3c.txt — точное
   совпадение всех 8 метрик.
3. Сравнить callsites_before_batch3c.txt с callsites_after_batch3c.txt —
   набор мест вызова идентичен (с учётом ожидаемых изменений из Этапа 2.2,
   если CustomRadioButton был объединён — явно выделить и объяснить).
4. SHA256 всех файлов, кроме Supervertaler.py и modules/styled_widgets.py —
   должны совпасть с manifest_before_batch3c.txt.
5. ВИЗУАЛЬНОЕ/ПОВЕДЕНЧЕСКОЕ СРАВНЕНИЕ (аналог DOCX round-trip из Batch #2,
   критично именно для этого батча):
   - Открыть вкладки TM, Termbase, AI-настройки (и любые другие, найденные
     в 1.5) на коде ДО батча (git HEAD) и ПОСЛЕ — сравнить внешний вид
     каждого чекбокса/радиокнопки: цвет, размер, состояние
     checked/unchecked/hover/disabled.
   - Если скриншот-сравнение недоступно агенту — точное текстовое описание
     (hex-цвета, размеры в px) до/после, оба варианта должны совпасть.
6. Обычный smoke-тест (как в предыдущих батчах).

═══════════════════════════════════════════════════════════════
ЭТАП 4 — ОТЧЁТ (обязательный формат)
═══════════════════════════════════════════════════════════════
- Результат Этапа 1.3: таблица "класс монолита → потенциальное соответствие
  в styled_widgets.py".
- Результат Этапа 1.4 (КЛЮЧЕВОЙ ПУНКТ): для каждой из 4 пар — явное
  подтверждение идентичности-по-цвету с указанием числа тестовых сценариев
  и что именно сравнивалось (включая отдельный явный вывод по
  CustomRadioButton vs CheckmarkRadioButton — объединены или остались
  раздельными и почему). Если было расхождение и остановка — описать
  отдельно, это корректная работа протокола, не провал отчёта.
- Diff stat (git diff --stat).
- Callsites diff с объяснением ожидаемых изменений.
- Counts diff — все 8 метрик.
- SHA256 diff — какие файлы изменились (ожидается 2: Supervertaler.py +
  modules/styled_widgets.py).
- Результат визуального/поведенческого сравнения (Этап 3.5).
- Результат обычного smoke-теста.
- "Непокрытые проверки" — если что-то не удалось прогнать end-to-end,
  явный список: что / почему / риск / как проверить руками (для внесения
  в VALIDATION_BACKLOG.md). Если таких пунктов нет — явно написать "нет".
- Явное решение по 1.5 (сохранены ли call sites текстуально) и почему.
- Явный GO/NO-GO с обоснованием.
- Один коммит: "Merge checkbox/radio button widgets into
  modules/styled_widgets.py (Batch #3c, Step 3 of EXTRACTION_PLAN.md)".

СТОП-УСЛОВИЯ:
1. Если Этап 1.4 находит поведенческое расхождение между любой из 4 пар —
   остановиться немедленно, показать конкретный расходящийся пример, ждать
   решения владельца. Не выбирать версию/объединение самостоятельно.
2. Если фактический AST-состав классов (1.1) отличается от заявленного
   скоупа — остановиться, уточнить скоуп.
3. После Этапа 4 — остановиться. Это последний под-батч Step 3
   EXTRACTION_PLAN.md целиком — ждать подтверждения владельца перед
   переходом к Step 4 (чистые QThread-воркеры, будущий Batch #4). Не
   продолжать автоматически.
