РОЛЬ: Ты выполняешь ТОЛЬКО инвентаризацию и анализ в проекте Supervertaler.
Режим: READ-ONLY ANALYSIS. В этом этапе НЕ ДОПУСКАЕТСЯ ни одного изменения
кода — ни в Supervertaler.py, ни где-либо ещё. Если в процессе анализа
возникнет соблазн "заодно поправить" что-то — не делать, зафиксировать в
отчёте как отдельное наблюдение.

ПРЕДПОСЫЛКА: Batch #4 (Step 4, QThread-воркеры) подтверждён GO — и
независимой проверкой (SHA256/AST), и владельцем на живом приложении.
Продолжаем от текущего HEAD (коммит "Extract QThread workers into
modules/workers/ (Batch #4, Step 4 of EXTRACTION_PLAN.md)").

ПОЧЕМУ ЭТОТ БАТЧ РАЗБИТ НА ДВА САМОСТОЯТЕЛЬНЫХ ЭТАПА-ПРОМПТА (Stage 1 / Stage
2), в отличие от предыдущих: все батчи #1–#4 либо переносили один непрерывный
блок, либо цельные классы без MainWindow-связности. Step 5 — принципиально
другой случай:
- Перемещаемые единицы — bound-методы самого SupervertalerQt (не отдельные
  классы), которые читают/меняют состояние через self.
- По предварительному разбору (см. ГИПОТЕЗЫ ниже) они физически НЕ лежат
  единым блоком, а перемешаны с 13+ чужими методами, которые остаются в
  монолите.
- Минимум 2 из переносимых методов — подтверждённые DYNAMIC_ENTRY_POINT
  (см. ниже), то есть простое "вырезать и импортировать" может сломать
  динамический вызов.
- EXTRACTION_PLAN.md прямо оставляет архитектурный вопрос открытым
  ("оставить как делегаты (переходный период) ИЛИ заменить все call-sites") —
  это решение владельца по прецеденту Batch #2/#3c, не то, что агент решает
  сам по ходу переноса.
Поэтому: Stage 1 — только собрать факты и подготовить это решение владельцу
максимально предметно. Stage 2 (сам перенос) будет отдельным промптом,
подготовленным ПОСЛЕ того, как владелец увидит отчёт Stage 1 и подтвердит
архитектуру. Если контекст текущей сессии агента к концу Stage 1 сильно
заполнен — перед стартом Stage 2 использовать /compact (или начать заново:
/goal у ZCode, /newtask у Cline) — Stage 2 в любом случае будет
самодостаточным промптом, заново переустанавливающим все факты, а не
полагающимся на память текущей сессии.

СКОУП STAGE 1 (и ничего больше): собрать и проверить факты о Step 5
EXTRACTION_PLAN.md ("Undo-менеджер") и выдать владельцу отчёт с рекомендацией
по архитектуре. Ноль изменений кода.

ГИПОТЕЗЫ ДЛЯ ПРОВЕРКИ (собраны из EXTRACTION_PLAN.md + CODE_MAP_REFACTOR.md +
DEAD_CODE_REPORT.md владельцем/координатором ДО этого промпта — считать
рабочей отправной точкой, НЕ фактом; каждый пункт обязателен к проверке через
AST/grep, документам не доверять дословно):

- EXTRACTION_PLAN.md утверждает "7 методов" в диапазоне
  "12493–13066 + 12851–12886". CODE_MAP_REFACTOR.md перечисляет их как 8
  отдельных строк таблицы:
  record_undo_state (12493–12521), record_undo_states_batch (12523–12567),
  undo_action_handler (12569–12583), redo_action_handler (12585–12598),
  _apply_undo_redo_action (12600–12635), _push_structural_undo
  (12851–12866), _apply_structural_history (13008–13059),
  update_undo_redo_actions (13061–13064).
  Это расхождение (7 vs 8) — не проверено, требует разрешения через AST.

- DEAD_CODE_REPORT.md помечает undo_action_handler (12569) и
  redo_action_handler (12585) как DYNAMIC_ENTRY_POINT с тегами
  EXTERNAL_ATTR_REF, HASATTR_REF, MENU_ACTION, STRING_REF. Остальные 6
  методов в DEAD_CODE_REPORT.md по имени не встречаются (предварительно —
  не DYNAMIC_ENTRY_POINT, но проверить явно, документ мог устареть).

- По CODE_MAP_REFACTOR.md, в диапазоне 12493–13109 физически присутствуют (в
  порядке следования, ПРЕДПОЛОЖИТЕЛЬНО не входят в скоуп Step 5 — задача
  Stage 1 подтвердить это через AST, а НЕ просто поверить списку):
  _segment_for_grid_row (12638–12653), _split_segment_at_row (12655–12696),
  _merge_segment_at_row (12698–12732), _delete_segments_at_rows
  (12734–12782), delete_current_segments (12784–12806), split_current_segment
  (12809–12838), merge_current_segment (12840–12849), _sync_after_structural
  (12868–12884), _recompute_list_numbers (12886–12913),
  _reindex_grid_rows_from (12915–12936), _select_grid_row_by_id
  (12938–12959), _split_segment_grid_fast (12961–12987),
  _merge_segment_grid_fast (12989–13006), create_quick_access_toolbar
  (13066–13109). Итого предварительно ~14 чужих методов, разбросанных между
  и вокруг переносимых.

- _sync_after_structural (12868–12884) — пограничный случай: вызывается теми
  же тремя методами (_split/_merge/_delete_segments_at_rows), что вызывают и
  _push_structural_undo (который ПЕРЕНОСИТСЯ), но сам не входит в список "7
  методов" плана. Требует отдельного решения (см. задачу 1.5 ниже).

ЖЁСТКИЕ ЗАПРЕТЫ (для Stage 1):
- Не менять ни одной строки кода нигде в репозитории.
- Не создавать modules/undo_manager.py и не резервировать это имя каким-либо
  файлом-заглушкой — это задача Stage 2.
- Не принимать архитектурное решение (делегаты vs замена call sites) самому
  — только собрать факты и дать обоснованную рекомендацию, финальное решение
  за владельцем.
- Не пропускать пункты 1.1–1.9 ниже, даже если что-то кажется очевидным по
  документам — именно расхождения с документами (7 vs 8, состав диапазона)
  и есть предмет этого этапа.

═══════════════════════════════════════════════════════════════
ЗАДАЧИ STAGE 1
═══════════════════════════════════════════════════════════════

1.1 ПОЛНАЯ AST-ИНВЕНТАРИЗАЦИЯ ДИАПАЗОНА
- Через ast.parse/lineno/end_lineno перечислить АБСОЛЮТНО ВСЕ методы класса
  SupervertalerQt (и, если есть, вложенные/локальные функции) в диапазоне
  строк примерно 12480–13115 (с запасом по краям от заявленных 12493 и
  13066/13109).
- Составить таблицу: имя метода → фактические (start, end) → предполагаемый
  статус (MOVE / NOT MOVE / НЕИЗВЕСТНО) по гипотезе выше → совпадает ли с
  CODE_MAP_REFACTOR.md (да/нет/иначе).
- Явно указать любые методы, найденные фактически, но отсутствующие в
  гипотезе выше (устаревшая документация, новые методы с момента написания
  CODE_MAP_REFACTOR.md) — не отбрасывать, включить в таблицу.

1.2 РАЗРЕШЕНИЕ ПРОТИВОРЕЧИЯ "7 vs 8"
- На основе 1.1 дать окончательный, проверенный список методов на перенос.
  Явно написать: "подтверждено N методов: [список]" — с обоснованием, почему
  именно столько (не механически "потому что EXTRACTION_PLAN.md сказал 7" и
  не механически "потому что таблица показала 8" — а по факту AST-анализа).

1.3 СНИМКИ "ЧУЖИХ" МЕТОДОВ (аналог PreTranslationWorker в Batch #4)
- Для каждого метода в диапазоне, отнесённого к NOT MOVE (по факту 1.1, не
  по гипотезе) — снять точный байтовый блок по AST-границам и посчитать
  SHA256. Сохранить в
  docs/refactoring/audits/not_moved_methods_batch5_snapshot.txt (имя →
  границы → SHA256 → размер в байтах). Это будет контрольная точка для
  Stage 2 — там потребуется байт-в-байт подтверждение, что ни один из этих
  методов не задет при удалении соседних переносимых методов.

1.4 МЕХАНИЗМ DYNAMIC_ENTRY_POINT ДЛЯ undo_action_handler / redo_action_handler
- Найти ФАКТИЧЕСКИЕ места, откуда эти два метода достижимы динамически:
  меню (QAction, как оно создаётся и с чем связывается — по имени строкой,
  через getattr, через lambda с прямой ссылкой на метод?), hasattr-проверки,
  строковые ссылки (grep по "undo_action_handler"/"redo_action_handler" как
  строки в кавычках), любые EXTERNAL_ATTR_REF-паттерны.
- Ответить явно: обязательно ли имя self.undo_action_handler (и
  self.redo_action_handler) должно продолжать существовать как bound-метод
  ИМЕННО на экземпляре SupervertalerQt после переноса, или достаточно, чтобы
  функциональность была доступна как-то иначе (например, через прямой вызов
  на объекте undo-менеджера)? Дать однозначный вывод с указанием конкретных
  строк кода, а не общими словами.

1.5 ПОГРАНИЧНЫЙ СЛУЧАЙ _sync_after_structural
- Прочитать тело метода целиком. Определить: обращается ли он к
  undo_stack/redo_stack/max_undo_levels напрямую, или его работа
  ограничена обновлением грида/статистики/заголовка окна (то есть он по
  смыслу ближе к будущему modules/segments_controller или
  modules/grid/helpers, а не к undo-подсистеме)?
- Дать рекомендацию (не решение): остаётся в монолите как есть (по
  умолчанию, раз не входит в список 7/8 методов плана) — или требует
  включения в скоуп Step 5 — с явным обоснованием в обоих случаях.

1.6 ПОЛНАЯ ИНВЕНТАРИЗАЦИЯ CALL SITES
- Grep/AST по всему репозиторию (Supervertaler.py + modules/*.py) на все
  вызовы каждого из подтверждённых в 1.2 методов. План ожидает "23+ места" —
  подтвердить фактическое число.
- Для КАЖДОГО call site пометить: вызывается ли он как self.ИмяМетода(...)
  (bound-вызов на экземпляре SupervertalerQt) или как-то иначе (прямой вызов
  функции, вызов через другой объект и т.п.). Это напрямую определяет, нужен
  ли делегат-обёртка для сохранения текстуальной идентичности вызовов.
- Отдельно выделить call sites из методов, которые САМИ остаются в монолите
  (например, если _split_segment_at_row вызывает self._push_structural_undo(...)
  — подтвердить фактически, это ключевой аргумент для решения по архитектуре).

1.7 ЗАВИСИМОСТИ / КОЛБЭКИ
- EXTRACTION_PLAN.md утверждает: методы читают current_project, table; для
  работы им нужны update_window_title, update_progress_stats,
  _update_status_cell, load_segments_to_grid.
- Построить точную таблицу: метод → какие из этих (и любых иных, не
  упомянутых в плане) атрибутов/методов MainWindow он фактически использует
  — по телу метода, не по документу.

1.8 СОСТОЯНИЕ undo_stack / redo_stack / max_undo_levels
- Найти, где эти три атрибута инициализируются (скорее всего __init__ или
  отдельный setup-метод) — точные строки.
- Проверить ФАКТИЧЕСКИ (не предполагать): используются ли они где-либо ЕЩЁ,
  кроме 7/8 методов на перенос? В частности — сериализуются/упоминаются ли
  при сохранении/загрузке .svproj (to_dict/from_dict, json.dump/load)? Если
  да — это критично для Stage 2 (перенос состояния наружу класса потребует
  аккуратной точки доступа при save/load, а не только при undo/redo).

1.9 РЕКОМЕНДАЦИЯ ПО АРХИТЕКТУРЕ (обязательный вывод, не решение агента)
Рабочая гипотеза координатора, которую нужно ПОДТВЕРДИТЬ ИЛИ ОПРОВЕРГНУТЬ
фактическим анализом 1.4/1.6 (не просто согласиться по умолчанию):
"Единообразные делегаты-обёртки для ВСЕХ подтверждённых в 1.2 методов
(а не выборочно и не полная замена 23+ call sites), потому что (а) минимум 2
метода обязаны остаться bound-методами SupervertalerQt из-за
DYNAMIC_ENTRY_POINT, и (б) часть внутренних call sites — тоже self.-стиль из
методов, которые сами остаются в монолите."
- Если 1.4/1.6 подтверждают гипотезу — зафиксировать явно, с числом
  подтверждающих примеров.
- Если находки говорят иное (например, все 23+ call sites можно безопасно
  переписать, а DYNAMIC_ENTRY_POINT можно решить иначе) — сообщить это прямо
  и предложить альтернативу с обоснованием. Не подгонять факты под гипотезу.

═══════════════════════════════════════════════════════════════
СТОП-УСЛОВИЕ STAGE 1
═══════════════════════════════════════════════════════════════
После завершения 1.1–1.9 — ОСТАНОВИТЬСЯ. Не создавать modules/undo_manager.py,
не трогать Supervertaler.py. Собрать отчёт (формат ниже) и ждать
подтверждения владельца по архитектуре и составу переносимых методов. Stage 2
(сам перенос) будет отдельным промптом, запущенным только после этого
подтверждения — при необходимости в новой сессии агента.

═══════════════════════════════════════════════════════════════
ФОРМАТ ОТЧЁТА STAGE 1
═══════════════════════════════════════════════════════════════
- Полная таблица 1.1 (метод → границы → MOVE/NOT MOVE → совпадение с
  CODE_MAP_REFACTOR.md).
- Явное разрешение "7 vs 8" (1.2) с итоговым списком переносимых методов.
- Путь к файлу снимков 1.3 (not_moved_methods_batch5_snapshot.txt) и
  количество зафиксированных методов.
- Результат 1.4: конкретный механизм DYNAMIC_ENTRY_POINT для
  undo_action_handler/redo_action_handler, с точными строками кода.
- Рекомендация по 1.5 (_sync_after_structural) с обоснованием.
- Полный список call sites (1.6) — ожидается 23+, с пометкой self.-стиль/не
  self.-стиль по каждому.
- Таблица 1.7 (метод → используемые колбэки/атрибуты MainWindow).
- Результат 1.8 (сериализация undo-состояния в .svproj — да/нет, детали).
- Рекомендация по архитектуре (1.9) — подтверждена или опровергнута, с
  обоснованием и числом примеров.
- Явный вывод: "ГОТОВ К STAGE 2" или "ЕСТЬ БЛОКЕРЫ/ОТКРЫТЫЕ ВОПРОСЫ" — со
  списком, что именно требует решения владельца перед стартом переноса.
- Подтверждение: ни одна строка кода не изменена (git status чист, кроме
  файла снимков в docs/refactoring/audits/ и, если создан, самого отчёта).
