РОЛЬ: Ты выполняешь механический перенос кода в проекте Supervertaler.
Режим: ACTIVE REFACTOR. Факты ниже — новые находки координатора ПОСЛЕ отчёта
Stage 1 (Stage 1 не давал точных границ для pagination.py, только оценку
диапазона). Короткая AST-спот-проверка в начале всё равно обязательна.

ПРЕДПОСЫЛКА: Batch #6 Stage 2 (helpers.py) подтверждён GO — независимой
проверкой координатора и владельцем на живом приложении. Продолжаем от HEAD
(коммит "Extract grid helpers into modules/grid/helpers.py (Batch #6 Stage 2,
Step 6 of EXTRACTION_PLAN.md)"). Обязательно прочитать
docs/refactoring/reports/ОТЧЁТ Batch #6 Stage 1.txt (общий контекст, карта
цикла SCC#1) и docs/refactoring/reports/ОТЧЁТ Batch #6 Stage 2.txt (образец
качества/формата, архитектурные решения уже принятые для соседнего файла
того же пакета).

ЭТО ВТОРОЙ ИЗ ТРЁХ ПОД-БАТЧЕЙ STEP 6: Stage 2 = helpers.py (готово) → Stage 3
= pagination.py (этот промпт) → Stage 4 = filters.py (следующий).

ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ О МЕТОДОЛОГИИ (координатор сам чуть не ошибся на этом
шаге): простой поиск по имени функции ПО ВСЕМУ ФАЙЛУ находит ложные
совпадения — в Supervertaler.py есть вложенные локальные функции с
ИДЕНТИЧНЫМИ именами методов класса (например, `def go_to_page(page_index)`
внутри мастера первого запуска на строке ~6908 — это ЛОКАЛЬНАЯ функция
навигации по страницам QStackedWidget визарда, НЕ имеет отношения к гриду;
`def on_page_size_changed(text)` внутри диалога управления терминологией на
строке ~17625 — тоже локальная функция, другая фича). Единственный надёжный
способ найти настоящие 14 методов пагинации — искать ТОЛЬКО среди ПРЯМЫХ
методов класса SupervertalerQt (node.body класса, не ast.walk по всему
дереву). Все границы ниже уже получены координатором именно так — но
ПЕРЕПРОВЕРИТЬ самостоятельно тем же способом обязательно (Этап 0).

СКОУП STAGE 3 (и ничего больше): перенести 14 методов в НОВЫЙ файл
modules/grid/pagination.py. Архитектура — по образцу helpers.py (функции с
явными параметрами, НЕ класс — тот же принцип, что был решён и обкатан в
Stage 2), С ОДНИМ ВАЖНЫМ ДОПОЛНЕНИЕМ: там, где pagination.py нуждается в
том, что УЖЕ есть в modules/grid/helpers.py (тот же пакет, тот же уровень) —
импортировать функцию напрямую из helpers.py, а НЕ заводить для неё
отдельный колбэк-параметр. Это снижает число колбэков и соответствует
слоистой архитектуре, одобренной в Stage 1 §7.

СОСТАВ (14 методов, ВСЕ являются ПРЯМЫМИ методами класса SupervertalerQt,
образуют ЕДИНЫЙ непрерывный блок 28198–28717 — координатор перепроверил
специально, в отличие от Step 5/Stage-2-helpers здесь методы НЕ разбросаны):

_maybe_auto_set_page_size 28198–28233, _get_total_pages 28235–28242,
_update_pagination_ui 28244–28304, _apply_pagination_to_grid 28306–28427,
[ПРОПУСК 28429–28447 — НЕ ПЕРЕНОСИТСЯ], _start_background_populate
28449–28499 [НЕ ПЕРЕНОСИТСЯ — Step 8, populate-кластер],
_background_populate_step 28501–28543 [НЕ ПЕРЕНОСИТСЯ — Step 8],
go_to_first_page 28545–28551, go_to_prev_page 28553–28559, go_to_next_page
28561–28568, select_range_page_up 28570–28602, select_range_page_down
28604–28637, _select_range_between 28639–28658, _clear_selection_anchor
28660–28663, go_to_last_page 28665–28672, go_to_page 28674–28693,
on_page_size_changed 28695–28717.

Сразу перед блоком (28195–28196, ВНУТРИ класса, НЕ внутри какого-либо
метода) — два class-level константы: `PAGE_AUTO_ALL_THRESHOLD = 1000`,
`PAGE_FALLBACK_SIZE = 200`. Используются ТОЛЬКО внутри
_maybe_auto_set_page_size (строки 28216–28220) — координатор проверил grep'ом
по всему файлу, других упоминаний нет. Перенести вместе с методом как
module-level константы в pagination.py; удалить две class-level строки из
SupervertalerQt (подтвердить в Этапе 0, что действительно нигде больше не
используются, прежде чем удалять).

НЕ ПЕРЕНОСЯТСЯ (2 метода, физически внутри блока, между
_apply_pagination_to_grid и go_to_first_page): _start_background_populate,
_background_populate_step — часть populate/render-кластера (Step 8, SCC#1),
запрещены планом на этом шаге.

═══════════════════════════════════════════════════════════════
ДОПОЛНИТЕЛЬНЫЕ НАХОДКИ КООРДИНАТОРА (landmines, требуют решения на Этапе 1)
═══════════════════════════════════════════════════════════════

А) _clear_selection_anchor, ПОХОЖЕ, МЁРТВЫЙ КОД. DEAD_CODE_REPORT.md прямо
   отмечает: "Единственная текстуальная ссылка в 125 файлах — собственный
   def" (не вызывается НИГДЕ, даже через .connect). Собственный grep
   координатора по `.method(`-стилю подтверждает: 0 прямых вызовов.
   РЕШЕНИЕ (не обсуждается, стандартная политика проекта): переносится КАК
   ЕСТЬ, мёртвым, вместе с остальными 13 — "не удалять и не помечать
   dead-код" остаётся в силе даже для явно мёртвого кода при рефакторинге;
   решение удалить/пометить — отдельный вопрос владельца, не в скоупе этого
   батча. В отчёте явно подтвердить: 0 call sites найдено, перенесено как
   мёртвый код без изменений.

Б) _selection_anchor_row — РАЗДЕЛЯЕМЫЙ атрибут С КОДОМ ВНЕ ЭТОГО БАТЧА.
   Паттерн hasattr/getattr/setattr/del используется в select_range_page_up,
   select_range_page_down, _clear_selection_anchor (все три — в скоупе) — НО
   ТАКЖЕ напрямую устанавливается в on_cell_clicked (строка ~48789 на
   момент координаторской проверки, ПЕРЕПРОВЕРИТЬ фактическую строку) —
   обычный обработчик клика по ячейке, ОСТАЁТСЯ в монолите, НЕ в скоупе
   Step 6. `self._selection_anchor_row = row` там — простое присваивание
   атрибута, не вызов метода, так что on_cell_clicked НЕ требует изменений
   вообще (атрибут как жил на self, так и живёт). Для трёх переносимых
   методов: hasattr/getattr/setattr/del-логика — self-специфична, остаётся
   В ДЕЛЕГАТАХ (аналог decision по _get_line_edit_text в Stage 2); pure-
   функции получают текущее значение (или None/сентинел, если атрибута нет)
   параметром и возвращают новое значение или решение, что делегат применяет
   через setattr/del.

В) _page_size_decided_for — простой memoization-флаг (getattr/self.attr=),
   используется ТОЛЬКО внутри _maybe_auto_set_page_size, нигде больше
   (проверить фактически). Тот же паттерн, что Б — self-специфичная
   read/write обёртка в делегате, pure-функция получает текущее значение
   параметром, возвращает новое (или None, если не нужно менять).

Г) self.tr(...) — используется дважды внутри _update_pagination_ui (для
   текста меток pagination_label/total_pages_label). Pure-функция не может
   вызывать self.tr() — либо (i) передать tr-функцию колбэком (например,
   tr_callback=self.tr, тот же приём, что для log/go_to_page и др.), либо
   (ii) вернуть из pure-функции неформатированные данные и оставить
   self.tr()+setText в делегате. Выбрать и обосновать; предпочтение —
   вариант (i) для единообразия с остальными колбэками этого батча, но
   решение за агентом по факту чтения тела.

Д) ИМПОРТ ИЗ helpers.py ВМЕСТО КОЛБЭКА. _apply_pagination_to_grid зависит
   от _resize_visible_rows — этот метод уже перенесён в Stage 2
   (modules/grid/helpers.py, функция resize_visible_rows). Вместо колбэка
   на self._resize_visible_rows (что тоже сработало бы через делегат, но
   добавляет лишний слой) — pagination.py должен импортировать
   `from modules.grid.helpers import resize_visible_rows` напрямую и звать
   её как обычную функцию того же пакета, передавая нужные параметры
   (table, auto_resize_callback и т.д., как в её собственной сигнатуре из
   Stage 2 — свериться с docs/refactoring/audits/batch6_stage2_signatures.txt).
   Проверить, есть ли другие подобные случаи среди зависимостей 14 методов
   (что-то ещё, что уже есть в helpers.py) — не полагаться только на этот
   один найденный координатором пример.

Е) DYNAMIC_ENTRY_POINT — DEAD_CODE_REPORT.md (устаревшие номера строк, но
   имена и теги актуальны) помечает go_to_prev_page, go_to_next_page,
   on_page_size_changed тегом SIGNAL_SLOT/EXTERNAL_ATTR_REF/HASATTR_REF/
   STRING(_REF); select_range_page_up/select_range_page_down — ARG_REF.
   Проверить ВСЕ 14 фактически (включая те, что не упомянуты в grep
   координатора — DEAD_CODE_REPORT.md мог устареть) — но исходя из этого,
   ожидай, что минимум 5 методов требуют делегатов-обёрток с точным
   сохранением имени. Архитектурное решение по умолчанию (как в Step 5/
   Stage 2) — делегаты для ВСЕХ 14 единообразно, не выборочно.

═══════════════════════════════════════════════════════════════
ЖЁСТКИЕ ЗАПРЕТЫ
═══════════════════════════════════════════════════════════════
- Не переносить _start_background_populate, _background_populate_step —
  populate/render-кластер, Step 8, запрещено планом на этом шаге.
- Не трогать on_cell_clicked или любой другой код вне 14 методов, даже
  если он читает/пишет _selection_anchor_row.
- Не удалять и не помечать _clear_selection_anchor как dead — переносится
  как есть (см. пункт А).
- В pagination.py — не создавать класс, не хранить ссылку на окно — функции
  с явными параметрами (как в helpers.py).
- 14 делегатов в монолите — точные исходные имена/сигнатуры.
- Не переписывать call sites этих 14 методов за пределами их тел и
  создаваемых делегатов.
- Один шаг = один коммит.

═══════════════════════════════════════════════════════════════
ЭТАП 0 — БАЗОВАЯ ЛИНИЯ И СПОТ-ПРОВЕРКА
═══════════════════════════════════════════════════════════════
1. git status чист, продолжаем от указанного коммита.
2. AST-проверка границ — ОБЯЗАТЕЛЬНО только среди прямых методов класса
   SupervertalerQt (см. ПРЕДУПРЕЖДЕНИЕ выше), сверить с таблицей выше.
   Заодно подтвердить: между блоком нет ничего, кроме заявленных 2 methods
   populate-кластера — переисследовать диапазон 28190–28720 целиком.
3. Снять SHA256-снимки _start_background_populate и _background_populate_step
   (аналог PreTranslationWorker в Batch #4) →
   docs/refactoring/audits/not_moved_methods_batch6stage3_snapshot.txt.
4. Подтвердить фактически: PAGE_AUTO_ALL_THRESHOLD/PAGE_FALLBACK_SIZE,
   _page_size_decided_for, _selection_anchor_row — использованы ТОЛЬКО там,
   где указано выше (grep по всему репозиторию, не только по монолиту).
5. SHA256 всех .py файлов → manifest_before_batch6stage3.txt.
6. py_compile чисто.
7. baseline_counts_batch6stage3.txt (8 метрик).
8. Call sites всех 14 методов по всему репозиторию →
   callsites_before_batch6stage3.txt.

═══════════════════════════════════════════════════════════════
ЭТАП 1 — ФОКУС-PREFLIGHT
═══════════════════════════════════════════════════════════════
1.1 Решить пункт Г (tr-колбэк) — зафиксировать сигнатуру _update_pagination_ui.
1.2 Решить пункты Б/В (self-специфичные getattr/setattr обёртки в делегатах
    для select_range_page_up/select_range_page_down/_clear_selection_anchor
    и _maybe_auto_set_page_size) — таблица метод → что читает/пишет →
    сигнатура pure-функции → что делает делегат.
1.3 Найти ВСЕ случаи из пункта Д (что ещё из зависимостей 14 методов уже
    есть в modules/grid/helpers.py) — свериться с полным списком экспортов
    modules/grid/__init__.py.
1.4 Свести итоговую таблицу: метод → параметры pure-функции → откуда каждый
    берётся в делегате (self.attr / self.method как колбэк / import из
    helpers.py / module-level константа).
1.5 DYNAMIC_ENTRY_POINT — подтвердить фактически для всех 14 (пункт Е).

═══════════════════════════════════════════════════════════════
ЭТАП 2 — ПЕРЕНОС
═══════════════════════════════════════════════════════════════
1. Создать modules/grid/pagination.py. Добавить в modules/grid/__init__.py
   реэкспорт новых функций (расширить существующий файл, не переписывать
   вслепую — сохранить экспорт из helpers.py).
2. Перенести 14 тел как pure-функции по сигнатурам из 1.4, включая 2
   константы как module-level в pagination.py.
3. Импорт в Supervertaler.py: `from modules.grid import pagination as
   grid_pagination` (по образцу Stage 2 для helpers).
4. Удалить 2 class-level константы из SupervertalerQt (после подтверждения
   Этапа 0.4).
5. 14 делегатов на месте бывших тел, точные исходные имена/сигнатуры.
6. Убедиться, что _start_background_populate/_background_populate_step не
   тронуты (сверка по снимку Этапа 0.3).
7. Убедиться, что on_cell_clicked и весь остальной код, использующий
   _selection_anchor_row, не редактировались.

═══════════════════════════════════════════════════════════════
ЭТАП 3 — ВАЛИДАЦИЯ
═══════════════════════════════════════════════════════════════
1. py_compile Supervertaler.py + modules/grid/*.py — чисто.
2. 8 метрик vs baseline.
3. SHA256 всех файлов, кроме Supervertaler.py и pagination.py (+ изменённого
   __init__.py), — совпадают с manifest_before.
4. Байт-в-байт сверка _start_background_populate/_background_populate_step.
5. ФУНКЦИОНАЛЬНЫЕ СЦЕНАРИИ:
   - Переход по страницам (первая/пред/след/последняя/по номеру) на проекте
     с несколькими страницами.
   - Смена page size (включая переключение в "All" и обратно) —
     _maybe_auto_set_page_size на большом проекте (>1000 сегментов, если
     есть тестовые данные) автоматически откатывается на 200/страницу.
   - Shift+PageUp/PageDown — расширение выделения диапазона через страницы
     (select_range_page_up/down + _select_range_between), включая сброс
     анкера обычным кликом по ячейке (on_cell_clicked, НЕ переносился, но
     должен продолжать работать в связке).
   - Undo/Redo регрессия (Batch #5) — ПОВТОРИТЬ ещё раз: split/merge/delete
     → Undo (тот же сценарий, что в Stage 2, для очистки любых сомнений,
     раз _apply_pagination_to_grid — центральный узел, которым пользуются
     многие операции).
   - Метка "Segments X-Y of Z" и "of N" корректно обновляется (проверка
     решения по пункту Г — tr-колбэк работает).
6. Обычный smoke-тест.

═══════════════════════════════════════════════════════════════
ЭТАП 4 — ОТЧЁТ
═══════════════════════════════════════════════════════════════
Стандартный формат (как Stage 2): diff stat, counts diff, SHA256 diff, снимок
2 NOT MOVE методов PASS/FAIL, таблица 1.4 (итоговые сигнатуры — документация
для Stage 4/filters.py, которая тоже, вероятно, зависит от pagination.py),
чеклист функциональных сценариев, ОСОБО — Undo/Redo регрессия, результат
smoke, «Непокрытые проверки», явный GO/NO-GO, один коммит: "Extract grid
pagination into modules/grid/pagination.py (Batch #6 Stage 3, Step 6 of
EXTRACTION_PLAN.md)".

Если контекст сессии разрастётся — /compact (Zcode) или /smol (Cline) перед
Этапом 3.

СТОП-УСЛОВИЯ:
1. Этап 0.2 находит методы, расположенные НЕ как прямые методы класса (то
   есть повторяет ошибку координатора с ложными вложенными функциями) —
   остановиться, не включать такие "находки" в перенос.
2. _start_background_populate/_background_populate_step не совпадают по
   SHA256 после переноса — немедленный откат.
3. Undo/Redo регрессия (split/merge/delete → Undo) не проходит —
   немедленный откат, это кросс-батчевая регрессия сразу двух предыдущих
   батчей (#5 и #6 Stage 2).
4. После Этапа 4 — остановиться, ждать подтверждения владельца перед Stage 4
   (filters.py). Не продолжать автоматически.
