РОЛЬ: Ты выполняешь механический перенос кода в проекте Supervertaler,
с обязательным поведенческим сравнением ДО переноса. Режим: ACTIVE REFACTOR
с жёсткими стоп-условиями. Если pre-flight находит поведенческое расхождение
между дублирующимися реализациями — ОСТАНОВИСЬ и сообщи, не выбирай
"правильную" версию сам.

СКОУП BATCH #2 (и ничего больше):
Перенести в существующий modules/tag_manager.py DOCX tag-движок из
Supervertaler.py:
- ~30 функций верхнего уровня (диапазон строк 494–1767): runs_to_tagged_text,
  tagged_text_to_runs, strip_formatting_tags, strip_outer_wrapping_tags,
  compact_tags, expand_compact_tags, extract_*_tags, apply_formatting_tags,
  has_formatting_tags, get_docx_language, set_docx_language,
  get_formatted_html_display, validate_tag_transfer, place_tags_via_llm,
  count_pipe_symbols и другие pipe-хелперы, _normalize_ws_for_compare,
  _classify_tag, _tags_well_formed, _reinsert_tags_into_target.
- classmethod/staticmethod-хелперы класса SupervertalerQt:
  _strip_inline_tags, _raw_to_visible_offset, _wysiwyg_runs_to_tagged_text.

РЕШЕНИЕ ПО АРХИТЕКТУРЕ (уже принято владельцем, не пересматривать):
Расширяем существующий modules/tag_manager.py. НЕ создаём отдельный
tag_formatting.py. tag_manager.py "поглощает" версии из монолита.

ЖЁСТКИЕ ЗАПРЕТЫ:
- Не переименовывать функции/методы, не менять сигнатуры.
- Не трогать DYNAMIC_ENTRY_POINT методы.
- Не удалять и не помечать dead-код (strip_tags-дубли — отдельная, не эта
  задача; DEAD_CODE_REPORT §LIVE_BUT_LEGACY.11 явно откладывает это "после
  переноса tag-движка" — то есть на будущий шаг, не на этот).
- place_tags_via_llm НЕ должна получить прямую зависимость от llm_clients —
  сохранить текущий callback-паттерн как есть.
- Не чинить bare-import fallback в docx_handler.py:21-24
  (`from .tag_manager import` / `from tag_manager import`) — только
  зафиксировать, что перенос его не ухудшил. Нормализация импорта —
  отдельная задача.
- Один шаг = один коммит.

═══════════════════════════════════════════════════════════════
ЭТАП 0 — БАЗОВАЯ ЛИНИЯ
═══════════════════════════════════════════════════════════════
1. Убедиться, что рабочая копия чистая (git status), продолжить от коммита
   Batch #1.
2. SHA256 всех .py файлов → manifest_before_batch2.txt.
3. py_compile всех .py файлов — чисто.
4. Подсчитать baseline_counts_batch2.txt: .connect(, QShortcut(,
   create_shortcut, QTimer(, timeout.connect, singleShot,
   QMetaObject.invokeMethod — те же 8 метрик, что в Batch #1.

═══════════════════════════════════════════════════════════════
ЭТАП 1 — PRE-FLIGHT (не начинать перенос, пока весь этап не пройден)
═══════════════════════════════════════════════════════════════

1.1 ГРАНИЦЫ И СОСТАВ (AST, не доверять дословно числам из отчётов)
- Найти реальные границы каждой из ~30 функций верхнего уровня в диапазоне
  494–1767 через AST. Составить точный список имён и номеров строк —
  фактический, не переписанный из EXTRACTION_PLAN.md.
- Найти реальные границы 3 classmethod/staticmethod хелперов
  (_strip_inline_tags, _raw_to_visible_offset, _wysiwyg_runs_to_tagged_text)
  внутри класса SupervertalerQt.
- Если фактический список отличается от перечисленного выше (что-то не
  найдено, или найдено что-то дополнительное, явно относящееся к тегам,
  но не упомянутое) — сообщить и уточнить скоуп ПЕРЕД переносом, не решать
  самостоятельно включать/исключать.

1.2 ЧИСТОТА ФУНКЦИЙ
- Grep каждой из ~30 функций верхнего уровня на PyQt6/Qt-импорты,
  self.*-обращения, обращения к глобальным переменным монолита за пределами
  собственных параметров.
- Для 3 classmethod/staticmethod хелперов: подтвердить, что они не читают
  self (кроме cls, если applicable) — иначе это не чистый перенос, а нужен
  другой паттерн.

1.3 СОДЕРЖИМОЕ tag_manager.py — ПОЛНАЯ ИНВЕНТАРИЗАЦИЯ
- Прочитать modules/tag_manager.py целиком. Составить список всех функций/
  классов/методов, которые там уже есть.
- Для каждой функции монолита из скоупа (1.1) определить: (а) есть ли в
  tag_manager.py функция с тем же или похожим назначением, (б) если да —
  как называется.
- Особое внимание: runs_to_tagged_text и tagged_text_to_runs — DEAD_CODE_
  REPORT.md §10.2 уже фиксирует их как дубли монолит↔tag_manager. Составить
  явную пару "функция монолита ↔ функция tag_manager" для этих двух и любых
  других найденных дублей.

1.4 КРИТИЧЕСКИЙ ГЕЙТ — ПОВЕДЕНЧЕСКОЕ СРАВНЕНИЕ ДУБЛЕЙ
Это обязательный шаг. Не переходить к Этапу 2, пока он не завершён.
- Для каждой пары "функция монолита ↔ функция tag_manager" (найденной в 1.3)
  подготовить 5-8 тестовых входов: реальные фрагменты форматированного
  текста с bold/italic/underline, вложенными тегами, edge-case (пустая
  строка, только теги без текста, несбалансированные теги, если формат их
  вообще допускает).
- Прогнать оба варианта (монолит-версию и tag_manager-версию) на одинаковых
  входах, сравнить вывод.
- ВАЖНО: проверить, не существует ли уже готовый self-test/валидатор внутри
  самого кода — validate_tag_transfer (входит в скоуп переноса) по названию
  похожа именно на инструмент проверки соответствия тегов source/target.
  Изучить её сигнатуру и назначение ДО того, как писать сравнивающий код
  с нуля — возможно, её можно переиспользовать как готовый харнесс для
  этого сравнения.
- РЕЗУЛЬТАТ СРАВНЕНИЯ:
  * Если вывод идентичен на всех тестовых входах для всех найденных пар —
    зафиксировать это явно ("поведение идентично, подтверждено N тестами
    на пару") и продолжать к Этапу 2.
  * Если обнаружено ЛЮБОЕ расхождение хотя бы в одном входе хотя бы для
    одной пары — ОСТАНОВИТЬСЯ. Не решать самостоятельно, какая версия
    "правильная". Показать оба вывода на конкретном входе, где они
    разошлись, и ждать решения владельца. Это не техническая проблема —
    это вопрос о том, какое поведение приложение должно иметь, и это
    решение не относится к компетенции механического рефакторинга.

1.5 CALLSITES И CALLERS
- Grep по всему репозиторию (Supervertaler.py + modules/*.py) на вызовы
  каждой из ~30 функций и 3 classmethod-хелперов. Полный список → 
  callsites_before_batch2.txt.
- Отдельно выделить вызовы вида self._strip_inline_tags(...),
  self._raw_to_visible_offset(...), self._wysiwyg_runs_to_tagged_text(...) —
  посчитать их количество и места. Для classmethod/staticmethod при переносе
  в module-level функции эти call sites технически ломаются (self.method()
  перестаёт резолвиться), если не оставить делегат.
- РЕШЕНИЕ ПО УМОЛЧАНИЮ (можно отклониться с обоснованием): оставить в
  классе SupervertalerQt тонкие делегирующие методы вида
  `def _strip_inline_tags(self, ...): return tag_manager.strip_inline_tags(...)`
  вместо правки всех call sites напрямую. Это минимизирует диаметр
  изменений и упрощает откат. Указать в отчёте, сколько call sites это
  затрагивает и почему выбран этот путь (или другой).

1.6 ПРОВЕРКА НА DYNAMIC_ENTRY_POINT И СТРОКОВЫЕ ССЫЛКИ
- Проверить каждое имя из скоупа (1.1) на упоминание в getattr/hasattr/
  setattr, QMetaObject.invokeMethod, DYNAMIC_ENTRY_POINT списке
  (DEAD_CODE_REPORT.md). Ожидается, что для plain top-level функций
  результат отрицательный, но проверить явно, не полагаться на ожидание.

1.7 ЛЕНИВЫЕ ИМПОРТЫ ВНУТРИ ФУНКЦИЙ
- Проверить, есть ли внутри переносимых функций локальные import-выражения
  (по аналогии с _uuid/hashlib в Batch #1). Если есть — они переносятся
  вместе с телом функции как есть, без изменений.

═══════════════════════════════════════════════════════════════
ЭТАП 2 — ПЕРЕНОС (только после чистого прохождения Этапа 1.4)
═══════════════════════════════════════════════════════════════
1. Для функций без дубля в tag_manager.py: перенести тело вербатим в
   tag_manager.py.
2. Для функций С подтверждённо-идентичным дублем в tag_manager.py:
   НЕ дублировать. Удалить версию монолита, все callsites в монолите
   переключить на уже существующую версию из tag_manager.py (импорт).
   Явно зафиксировать в отчёте, какая версия (монолита или tag_manager)
   была оставлена как канонiческая и почему (обычно — которая уже
   в tag_manager, раз её и так вызывают другие модули).
3. Для 3 classmethod/staticmethod хелперов: перенести как module-level
   функции в tag_manager.py; в классе SupervertalerQt оставить тонкие
   делегаты (см. 1.5), если не выбран другой путь.
4. Добавить в Supervertaler.py импорт всех перенесённых имён из
   modules.tag_manager (одна строка или логичный блок сразу после
   существующего импорта tag_manager, если он там уже есть).
5. Удалить оригинальные определения из Supervertaler.py.

═══════════════════════════════════════════════════════════════
ЭТАП 3 — ВАЛИДАЦИЯ
═══════════════════════════════════════════════════════════════
1. py_compile всех файлов — чисто.
2. Сравнить baseline_counts_batch2.txt с counts_after_batch2.txt — точное
   совпадение всех 8 метрик.
3. Сравнить callsites_before_batch2.txt с callsites_after_batch2.txt — 
   набор мест вызова идентичен (с учётом ожидаемых изменений из Этапа 2.2,
   которые нужно явно выделить и объяснить, а не просто показать diff).
4. SHA256 всех файлов, кроме Supervertaler.py и modules/tag_manager.py —
   должны совпасть с manifest_before_batch2.txt.
5. DOCX ROUND-TRIP ТЕСТ (замена/дополнение обычного smoke-теста, критично
   именно для этого батча):
   - Взять 2-3 реальных DOCX-файла с разнообразным форматированием
     (bold, italic, underline, смешанные внутри одного предложения,
     если есть — списки/таблицы).
   - Импортировать в новый проект (import_docx_from_path).
   - Не редактируя перевод (или сделав минимальную правку одного сегмента),
     экспортировать обратно (export_target_only_docx).
   - Сравнить структуру форматирования исходного и экспортированного DOCX
     (не обязательно побайтово — DOCX это ZIP с XML, но сравнить, что теги
     форматирования на тех же словах сохранились: например, извлечь текст
     runs с их bold/italic флагами из document.xml до и после, сравнить).
   - Повторить то же самое на коде из git HEAD (до Batch #2) для тех же
     файлов — как контрольную группу, чтобы доказать: результат экспорта
     не изменился относительно поведения до рефакторинга.
6. Обычный smoke-тест (как в Batch #1): создание проекта → импорт →
   правка → подтверждение → сохранение → перезапуск → загрузка → проверка
   состояния.

═══════════════════════════════════════════════════════════════
ЭТАП 4 — ОТЧЁТ (обязательный формат)
═══════════════════════════════════════════════════════════════
- Результат Этапа 1.3: полная таблица "функция монолита → статус
  (уникальная / дубль tag_manager.имя)".
- Результат Этапа 1.4 (КЛЮЧЕВОЙ ПУНКТ): для каждой найденной пары дублей —
  явное подтверждение идентичности поведения с указанием числа тестовых
  входов и что именно тестировалось. Если было расхождение и остановка —
  описать её отдельно, это не провал отчёта, а корректная работа протокола.
- Diff stat (git diff --stat).
- Callsites diff с объяснением ожидаемых изменений (Этап 2.2/2.3).
- Counts diff — все 8 метрик.
- SHA256 diff — какие файлы изменились (ожидается 2: Supervertaler.py +
  modules/tag_manager.py).
- Результат DOCX round-trip теста — с контрольной группой на HEAD-версии.
- Результат обычного smoke-теста.
- Явное решение по 1.5 (делегаты vs прямая правка callsites) и почему.
- Явный GO/NO-GO с обоснованием.
- Один коммит: "Extract DOCX tag engine into modules/tag_manager.py
  (Batch #2, Step 2 of EXTRACTION_PLAN.md)".

СТОП-УСЛОВИЯ (расширенные относительно Batch #1):
1. Если Этап 1.4 находит поведенческое расхождение между дублями —
   остановиться немедленно, показать конкретный расходящийся пример,
   ждать решения владельца. Не выбирать версию самостоятельно.
2. Если фактический AST-состав функций (1.1) отличается от заявленного
   скоупа — остановиться, уточнить скоуп.
3. После Этапа 4 — остановиться и ждать подтверждения перед Batch #3.
   Не продолжать автоматически.