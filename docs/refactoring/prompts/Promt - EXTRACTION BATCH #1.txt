РОЛЬ: Ты выполняешь механический, атомарный перенос кода в проекте Supervertaler.
Режим: ACTIVE REFACTOR, но с обязательными pre-flight проверками. Если любая
pre-flight проверка провалится — ОСТАНОВИСЬ и сообщи о несоответствии, не
пытайся "исправить попутно".

СКОУП BATCH #1 (и ничего больше):
Перенести классы Comment, Segment, Project из Supervertaler.py в новый файл
modules/models.py. Это единственная задача. Tag-движок, диалоги, воркеры —
НЕ в этом батче.

ЖЁСТКИЕ ЗАПРЕТЫ:
- Не переименовывать классы, методы, атрибуты.
- Не менять сигнатуры.
- Не трогать DYNAMIC_ENTRY_POINT методы (список в DEAD_CODE_REPORT.md).
- Не удалять и не помечать dead-код — это отдельная задача, не в этом батче.
- Не чинить баг jump_to_segment (DEAD_CODE_REPORT.md §UNKNOWN.3) — оставить как есть.
- Не создавать modules/utils.py — только modules/models.py.
- Один шаг = один коммит. Не смешивать с другими правками.

ЭТАП 0 — БАЗОВАЯ ЛИНИЯ (обязательно перед любым изменением кода):
1. Если нет git-репозитория — выполнить `git init`, закоммитить текущее
   состояние как есть с сообщением "baseline before extraction batch 1".
2. Сохранить SHA256 всех .py файлов проекта в manifest_before.txt.
3. Прогнать `python -m py_compile` по всем .py файлам — зафиксировать, что
   компиляция чистая ДО изменений.
4. Посчитать и зафиксировать текущие числа: количество `.connect(`, количество
   QShortcut/ShortcutManager.create_shortcut регистраций, количество
   QTimer.timeout/singleShot вызовов, количество QMetaObject.invokeMethod.
   Сохранить как baseline_counts.txt.

ЭТАП 1 — НЕЗАВИСИМАЯ ПЕРЕПРОВЕРКА (не доверять цифрам из отчётов дословно):
1. Найти точные границы классов Comment, Segment, Project в Supervertaler.py
   через AST (не текстовый grep по номеру строки из отчёта — перепроверить
   реальные `class X:` и конец тела класса).
2. Посчитать реальное число методов каждого класса и сравнить с ожидаемым
   (Comment ~6, Segment ~9, Project ~3+class-level атрибуты). Если расхождение
   — сообщить и остановиться, не переносить "на глаз".
3. Grep по диапазону строк этих трёх классов на `PyQt6`, `from PyQt6`,
   `import PyQt6` — подтвердить отсутствие Qt-зависимостей. Если найдено —
   ОСТАНОВИТЬСЯ, класс не "чистый", перенос требует пересмотра.
4. Проверить наличие мутабельных class-level атрибутов у Project (не dataclass
   field(default_factory=...), а голый list/dict/set на уровне класса).
   Зафиксировать как есть (не чинить), просто задокументировать в отчёте.
5. Найти механизм сериализации .svproj: grep по всему репозиторию на
   `pickle.dump`, `pickle.load`, `pickle.dumps`, `pickle.loads` в контексте
   сохранения/загрузки проекта. Если .svproj сериализуется через pickle —
   ОСТАНОВИСЬ немедленно и сообщи об этом человеку. Перенос класса в другой
   модуль при pickle-сериализации сломает загрузку существующих файлов
   пользователей (pickle привязывает объект к __module__). Если сериализация
   идёт через to_dict/from_dict + JSON — можно продолжать.
6. Grep всего repo (Supervertaler.py + modules/*.py) на литералы `Comment(`,
   `Segment(`, `Project(`, `class Comment`, `class Segment`, `class Project`,
   а также любые forward-reference type hints вида `'Segment'`, `'Project'`,
   `'Comment'` внутри modules/. Сохранить полный список site'ов как
   callsites_before.txt.

ЭТАП 2 — ПЕРЕНОС:
1. Создать modules/models.py.
2. Перенести тела трёх классов ВЕРБАТИМ (без изменений форматирования,
   без "улучшений", без добавления type hints, если их не было).
3. В Supervertaler.py добавить одну строку импорта сразу после существующего
   блока импортов modules.*: `from modules.models import Comment, Segment, Project`
4. Удалить оригинальные определения классов из Supervertaler.py.
5. Убедиться, что все внутренние вызовы `Comment(...)`, `Segment(...)`,
   `Project(...)` внутри Supervertaler.py остались текстуально идентичными —
   меняется только откуда класс импортирован, не как он вызывается.

ЭТАП 3 — ВАЛИДАЦИЯ (обязательна, отчитаться по каждому пункту):
1. `python -m py_compile` на Supervertaler.py и modules/models.py — чисто.
2. Повторить подсчёт .connect/.shortcut/.timer/.invokeMethod — сравнить с
   baseline_counts.txt, числа ДОЛЖНЫ совпасть точно.
3. Повторить grep callsites (Comment(/Segment(/Project() — сравнить с
   callsites_before.txt, набор мест вызова должен быть идентичен (только
   строка импорта новая).
4. SHA256 всех файлов, кроме Supervertaler.py и нового modules/models.py,
   должны совпасть с manifest_before.txt (изменились ТОЛЬКО эти два файла).
5. Запустить приложение. Выполнить: создание нового проекта → импорт DOCX или
   TXT → перевод/правка одного сегмента → подтверждение сегмента →
   сохранение .svproj → закрытие приложения → повторный запуск → открытие
   того же .svproj → проверка, что сегмент/статус/комментарий сохранились.
6. РЕГРЕССИЯ ФАЙЛОВ: взять 2-3 существующих .svproj файла (сохранённых ДО
   этого изменения), открыть каждый, пересохранить как новый файл, сделать
   побайтовое сравнение старого и нового файла с игнорированием
   timestamp-полей. Расхождений в остальных полях быть не должно.
7. Если есть уже существующие проекты пользователей, специально проверить
   загрузку хотя бы одного файла, сохранённого ДО этого коммита, текущей
   (уже изменённой) версией кода — это ключевая проверка на п.5 Этапа 1.

ЭТАП 4 — ОТЧЁТ (обязательный формат, не просто "готово"):
- Diff stat (git diff --stat).
- Результат сравнения callsites_before.txt vs callsites_after.txt (diff).
- Результат сравнения baseline_counts.txt vs counts_after.txt (diff).
- Результат SHA256-сравнения (какие файлы изменились — должно быть ровно 2).
- Чеклист smoke-теста (п.5 Этапа 3) — пункт за пунктом, пройдено/не пройдено.
- Результат файловой регрессии .svproj (байт-в-байт, кроме timestamp).
- Явный вывод по механизму сериализации (pickle или to_dict/JSON) — что
  обнаружено, почему это не риск.
- Явное GO/NO-GO решение с обоснованием.
- Один git-коммит с сообщением: "Extract Comment/Segment/Project to
  modules/models.py (Batch #1, Step 1 of EXTRACTION_PLAN.md)".

СТОП-УСЛОВИЕ:
После Этапа 4 — остановиться и ждать подтверждения человека перед тем, как
переходить к Batch #2 (tag_formatting.py). Не продолжать автоматически.