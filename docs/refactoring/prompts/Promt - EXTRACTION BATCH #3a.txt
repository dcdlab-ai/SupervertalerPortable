РОЛЬ: Ты выполняешь механический, атомарный перенос кода в проекте Supervertaler.
Режим: ACTIVE REFACTOR, но с обязательными pre-flight проверками (как в Batch #1
и #2). Если любая pre-flight проверка провалится — ОСТАНОВИСЬ и сообщи о
несоответствии, не пытайся "исправить попутно".

Унаследовано из Batch #2: если pre-flight найдёт, что что-либо в этом скоупе
совпадает по названию/назначению с уже существующим кодом в modules/ — не
верь этому на слово, сравни поведение, и при любом расхождении ОСТАНОВИСЬ,
не выбирай "правильную" версию сам.

КОНТЕКСТ: Step 3 EXTRACTION_PLAN.md в оригинале охватывает event-фильтры +
диалоги + чекбоксы одним шагом. По решению владельца разбит на три
под-батча (3a/3b/3c), т.к. это три разных модуля-назначения с разным
уровнем риска. Диалоги (Batch #3b) и чекбоксы/слияние со styled_widgets.py
(Batch #3c) — НЕ в этом батче.

СКОУП BATCH #3a (и ничего больше):
Перенести 6 классов Qt event-фильтров из Supervertaler.py (диапазон строк
2403–2901) в НОВЫЙ файл modules/event_filters.py:
_QuitEventFilter, _CtrlReturnEventFilter, _GridArrowKeyEventFilter,
_WheelGuard, _LoneCtrlEventFilter, GridTableEventFilter.

ЖЁСТКИЕ ЗАПРЕТЫ:
- Не переименовывать классы, не менять сигнатуры конструкторов.
- Не менять порядок, места или аргументы вызовов installEventFilter(...)
  в монолите.
- Не трогать DYNAMIC_ENTRY_POINT методы (список в DEAD_CODE_REPORT.md).
- Не удалять и не помечать dead-код — это отдельная задача, не в этом батче.
- Не создавать modules/utils.py или любой другой сборный файл — только
  modules/event_filters.py.
- Один шаг = один коммит.

═══════════════════════════════════════════════════════════════
ЭТАП 0 — БАЗОВАЯ ЛИНИЯ
═══════════════════════════════════════════════════════════════
1. Убедиться, что рабочая копия чистая (git status), продолжить от коммита
   Batch #2.
2. SHA256 всех .py файлов → manifest_before_batch3a.txt.
3. py_compile всех .py файлов — чисто.
4. Подсчитать baseline_counts_batch3a.txt: те же 8 метрик, что в Batch #1/#2 —
   .connect(, QShortcut(, create_shortcut(, QTimer(, timeout.connect,
   QTimer.timeout, singleShot, QMetaObject.invokeMethod.

═══════════════════════════════════════════════════════════════
ЭТАП 1 — PRE-FLIGHT (не начинать перенос, пока весь этап не пройден)
═══════════════════════════════════════════════════════════════

1.1 ГРАНИЦЫ И СОСТАВ (AST, не доверять дословно числам из отчётов)
- Найти реальные границы каждого из 6 классов в диапазоне 2403–2901 через
  AST (не текстовый grep по номеру строки из CODE_MAP_REFACTOR.md).
- Если фактический список отличается от перечисленного выше (другой
  диапазон, пропущенный класс, что-то дополнительное явно относящееся к
  event-фильтрам, но не упомянутое) — сообщить и уточнить скоуп ПЕРЕД
  переносом, не решать самостоятельно включать/исключать.

1.2 ЧИСТОТА КЛАССОВ / ИНТЕРФЕЙС
- Для каждого из 6 классов: какой параметр принимает __init__
  (mw / main_window / parent)? Зафиксировать точную сигнатуру.
- Подтвердить, что класс не обращается к self/глобалам монолита сверх этого
  параметра.
- ОСОБОЕ ВНИМАНИЕ: _WheelGuard. По данным CODE_MAP_REFACTOR.md §13.3 он НЕ
  принимает mw, как остальные 5. Установить его реальный интерфейс по телу
  класса, не по описанию в документе.

1.3 СОДЕРЖИМОЕ modules/event_filters.py — ПОЛНАЯ ИНВЕНТАРИЗАЦИЯ
- Подтвердить, что файла modules/event_filters.py не существует. Если
  существует — прочитать целиком, что там уже есть, прежде чем что-либо
  добавлять.

1.4 КРИТИЧЕСКИЙ ГЕЙТ — ПОВЕДЕНЧЕСКОЕ СРАВНЕНИЕ "ДУБЛЕЙ"
Это обязательный шаг, даже если ожидается, что он будет пустым (переносим в
новый файл, не сливаем с существующим).
- Grep по всему modules/ на предмет класса/функции с тем же именем ИЛИ тем
  же назначением (обработка событий Qt, аналогичные фильтры), что любой из
  6 переносимых классов.
- Если что-то похожее найдено — не верить, что это "разное" или "то же
  самое" на слово. Сравнить поведение: подготовить 3-5 тестовых сценариев
  (конкретные Qt-события: KeyPress с конкретной клавишей, Wheel-событие,
  и т.п.) и прогнать оба варианта.
- Если найдено расхождение — ОСТАНОВИТЬСЯ, показать конкретный расходящийся
  сценарий, ждать решения владельца.
- Если ничего похожего не найдено — зафиксировать явно: "дублей не найдено,
  гейт формально пройден" — и продолжать к Этапу 2.

1.5 CALLSITES И CALLERS
- Grep по всему репозиторию (Supervertaler.py + modules/*.py) на:
  * прямые конструкторы каждого из 6 классов (ClassName(...));
  * все вызовы installEventFilter(...), устанавливающие любой из этих
    6 фильтров, — с указанием, на какой объект и в каком порядке.
  Полный список → callsites_before_batch3a.txt.
- Эти 6 классов — не методы SupervertalerQt, а отдельные top-level классы,
  поэтому по умолчанию делегаты (как в Batch #2 для classmethod-хелперов)
  НЕ требуются: замена — это просто смена источника импорта. Явно
  подтвердить в отчёте, что ни один из 6 классов не вызывается как
  self.ИмяКласса(...) нигде в коде (что было бы неожиданно и требовало бы
  пересмотра решения).

1.6 ПРОВЕРКА НА DYNAMIC_ENTRY_POINT И СТРОКОВЫЕ ССЫЛКИ
- Проверить каждое из 6 имён на упоминание в getattr/hasattr/setattr,
  QMetaObject.invokeMethod, DYNAMIC_ENTRY_POINT списке (DEAD_CODE_REPORT.md).
  Ожидается отрицательный результат, но проверить явно.

1.7 ЛЕНИВЫЕ ИМПОРТЫ ВНУТРИ КЛАССОВ
- Проверить, есть ли внутри тел 6 классов локальные import-выражения. Если
  есть — переносятся вместе с телом как есть, без изменений.

═══════════════════════════════════════════════════════════════
ЭТАП 2 — ПЕРЕНОС (только после чистого прохождения Этапа 1.4)
═══════════════════════════════════════════════════════════════
1. Создать modules/event_filters.py.
2. Перенести тела 6 классов ВЕРБАТИМ (без изменений форматирования, без
   "улучшений").
3. В Supervertaler.py добавить импорт сразу после существующего блока
   импортов modules.*:
   from modules.event_filters import (_QuitEventFilter, _CtrlReturnEventFilter,
   _GridArrowKeyEventFilter, _WheelGuard, _LoneCtrlEventFilter,
   GridTableEventFilter)
4. Удалить оригинальные определения классов из Supervertaler.py.
5. Убедиться, что все installEventFilter(...) вызовы и создания экземпляров
   этих классов остались текстуально идентичными — меняется только откуда
   класс импортирован.

═══════════════════════════════════════════════════════════════
ЭТАП 3 — ВАЛИДАЦИЯ (обязательна, отчитаться по каждому пункту)
═══════════════════════════════════════════════════════════════
1. python -m py_compile на Supervertaler.py и modules/event_filters.py —
   чисто.
2. Повторить подсчёт 8 метрик — сравнить с baseline_counts_batch3a.txt,
   числа ДОЛЖНЫ совпасть точно.
3. Повторить grep callsites — сравнить с callsites_before_batch3a.txt, набор
   мест вызова идентичен (только строка импорта новая).
4. SHA256 всех файлов, кроме Supervertaler.py и modules/event_filters.py,
   должны совпасть с manifest_before_batch3a.txt.
5. ФУНКЦИОНАЛЬНЫЕ СЦЕНАРИИ (прогнать все, пункт за пунктом):
   - Закрытие главного окна (_QuitEventFilter) с close-to-tray включённым
     и выключенным.
   - Ctrl+Enter в гриде перевода (_CtrlReturnEventFilter).
   - Навигация стрелками в гриде, включая переход между видимыми строками
     при активной пагинации (_GridArrowKeyEventFilter).
   - Прокрутка колёсиком над spin-box/combo-box на вкладках настроек —
     значение НЕ должно меняться от случайной прокрутки (_WheelGuard).
   - Одиночное нажатие Ctrl без сопутствующих клавиш (_LoneCtrlEventFilter)
     — поведение не изменилось относительно HEAD.
   - Взаимодействие с GridTableEventFilter в таблице сегментов (определить
     точное назначение по телу класса на этапе 1.1/1.2, в документах не
     детализировано).
6. Обычный smoke-тест (как в Batch #1/#2): создание проекта → импорт →
   правка сегмента → подтверждение → сохранение → перезапуск → загрузка →
   проверка состояния.

═══════════════════════════════════════════════════════════════
ЭТАП 4 — ОТЧЁТ (обязательный формат, не просто "готово")
═══════════════════════════════════════════════════════════════
- Результат Этапа 1.4: явное подтверждение "дублей не найдено" ЛИБО,
  если что-то найдено — результат сравнения с конкретными примерами.
- Diff stat (git diff --stat).
- Callsites diff (before/after) с объяснением ожидаемых изменений.
- Counts diff — все 8 метрик.
- SHA256 diff — какие файлы изменились (ожидается 2).
- Чеклист функциональных сценариев (Этап 3.5) — пункт за пунктом,
  пройдено/не пройдено.
- Результат обычного smoke-теста.
- Явное подтверждение по 1.5 (делегаты не требуются, т.к. классы не
  self-методы) — или иное решение с обоснованием.
- Явный GO/NO-GO с обоснованием.
- Один git-коммит: "Extract Qt event filters into modules/event_filters.py
  (Batch #3a, Step 3 of EXTRACTION_PLAN.md)".

СТОП-УСЛОВИЯ:
1. Если Этап 1.4 находит расхождение поведения с чем-то похожим в modules/
   — остановиться немедленно, показать конкретный расходящийся пример,
   ждать решения владельца. Не выбирать версию самостоятельно.
2. Если фактический AST-состав классов (1.1) отличается от заявленного
   скоупа — остановиться, уточнить скоуп.
3. После Этапа 4 — остановиться и ждать подтверждения человека перед тем,
   как переходить к Batch #3b (диалоги). Не продолжать автоматически.
