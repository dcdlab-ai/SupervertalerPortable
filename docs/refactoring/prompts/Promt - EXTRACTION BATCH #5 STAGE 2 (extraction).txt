РОЛЬ: Ты выполняешь механический перенос кода в проекте Supervertaler.
Режим: ACTIVE REFACTOR. Факты о скоупе ниже — ПОДТВЕРЖДЕНЫ дважды (агентом в
Stage 1 и независимо координатором через AST) и одобрены владельцем по всем
4 открытым вопросам отчёта Stage 1. Полная переинвентаризация границ НЕ
требуется — но короткая спот-проверка (Этап 0) обязательна: между Stage 1 и
Stage 2 мог пройти любой промежуток времени.

ПРЕДПОСЫЛКА: Продолжаем от коммита "Batch #5 Stage 1: Step 5 inventory and
architecture recommendation (read-only, no code changes)" (HEAD на момент
написания — c11e1bc). Перед началом работы ОБЯЗАТЕЛЬНО прочитать целиком
docs/refactoring/reports/ОТЧЁТ Batch #5 Stage 1.txt — все ссылки на "1.1",
"1.6", "1.7" и т.д. ниже указывают на разделы этого отчёта. Открытые вопросы
В1–В4 того отчёта закрыты владельцем: согласие по всем 4, в редакции,
рекомендованной агентом Stage 1 (см. АРХИТЕКТУРА ниже).

СКОУП STAGE 2 (и ничего больше):
- Перенести 8 методов в НОВЫЙ файл modules/undo_manager.py, класс
  UndoManager: record_undo_state, record_undo_states_batch,
  undo_action_handler, redo_action_handler, _apply_undo_redo_action,
  _push_structural_undo, _apply_structural_history, update_undo_redo_actions.
- Состояние undo_stack, redo_stack, max_undo_levels переезжает вместе с
  методами — становится состоянием экземпляра UndoManager, а не
  SupervertalerQt.
- На месте бывших тел этих 8 методов в SupervertalerQt остаются 8 тонких
  делегатов с ТЕМИ ЖЕ именами, перенаправляющих вызов в self._undo_manager.
- Инициализация в __init__ (сейчас строки 6212–6214: self.undo_stack = [],
  self.redo_stack = [], self.max_undo_levels = 100) заменяется на создание
  self._undo_manager = UndoManager(...) с колбэками.

НЕ ПЕРЕНОСИТСЯ (14 методов, физически перемешаны с переносимыми в диапазоне
9373–9989, полный список с AST-границами и SHA256 — уже зафиксирован в
docs/refactoring/audits/not_moved_methods_batch5_snapshot.txt):
_segment_for_grid_row, _split_segment_at_row, _merge_segment_at_row,
_delete_segments_at_rows, delete_current_segments, split_current_segment,
merge_current_segment, _sync_after_structural, _recompute_list_numbers,
_reindex_grid_rows_from, _select_grid_row_by_id, _split_segment_grid_fast,
_merge_segment_grid_fast, create_quick_access_toolbar.

АРХИТЕКТУРА (решение владельца, не пересматривать без веской причины):
- UndoManager получает зависимости через КОЛБЭКИ, переданные в конструктор —
  НЕ через ссылку на self.main_window/self.parent_app (это отличает его от
  event_filters.py, который держит main_window напрямую; здесь владелец явно
  выбрал колбэки, см. Stage 1 §1.9 и В1).
- Полный список зависимостей — таблица §1.7 отчёта Stage 1. Дублирую здесь
  для удобства (сверить с отчётом, не полагаться только на эту копию):
  update_window_title, update_progress_stats, _update_status_cell,
  apply_invisible_replacements, _find_row_for_segment, project_modified
  (атрибут-флаг), current_project (атрибут), table (атрибут-грид),
  _select_grid_row_by_id, _split_segment_grid_fast, _merge_segment_grid_fast,
  _original_segment_order (атрибут), load_segments_to_grid, refresh_preview,
  log, undo_action, redo_action (QAction-объекты меню).
- 43 call sites (28 self.-стиль из методов монолита + 11 MOVE→MOVE + 4
  внешних duck-typed) НЕ переписываются — все продолжают вызывать
  self.record_undo_state(...) / mw.undo_action_handler() и т.п. как раньше,
  просто теперь это делегат. Ни один из этих 43 сайтов не редактируется.
- _sync_after_structural остаётся в монолите как есть (решение §1.5,
  подтверждено — нулевая связность с undo-состоянием).

ЖЁСТКИЕ ЗАПРЕТЫ:
- Не трогать ни один из 14 NOT MOVE методов — ни строки, ни форматирование
  вокруг них.
- Не переписывать ни один из 43 call sites за пределами тел самих 8
  переносимых методов и создаваемых 8 делегатов.
- Не создавать в UndoManager ссылку на main_window/parent_app/self.window —
  только именованные колбэки по списку выше.
- Не менять внешне видимые сигнатуры 8 методов (имена и порядок параметров,
  которые вызывающий код передаёт) — делегаты обязаны быть текстуально
  прозрачны для всех 43 call sites.
- Не трогать undo_action/redo_action (QAction) в create_menus — они остаются
  атрибутами SupervertalerQt как есть, в UndoManager передаются как есть
  (объекты, не копии).
- Один шаг = один коммит.

═══════════════════════════════════════════════════════════════
ЭТАП 0 — БАЗОВАЯ ЛИНИЯ И СПОТ-ПРОВЕРКА
═══════════════════════════════════════════════════════════════
1. git status чист, продолжаем от указанного коммита Stage 1.
2. Быстрая (не полная) AST-проверка: пересчитать границы 8 MOVE-методов и
   сверить с отчётом Stage 1 §1.2. Если ХОТЬ ОДНА граница разошлась — СТОП,
   что-то изменилось с момента Stage 1, разобраться прежде чем продолжать
   (не пытаться угадать причину самостоятельно).
3. Сверить docs/refactoring/audits/not_moved_methods_batch5_snapshot.txt:
   для каждого из 14 NOT MOVE методов пересчитать SHA256 по текущему файлу и
   сравнить со снимком. Все 14 обязаны совпасть. Расхождение — СТОП.
4. SHA256 всех .py файлов → manifest_before_batch5stage2.txt.
5. py_compile всех .py файлов — чисто.
6. baseline_counts_batch5stage2.txt — те же 8 метрик, что и в предыдущих
   батчах.

═══════════════════════════════════════════════════════════════
ЭТАП 1 — ФОКУС-PREFLIGHT (не переинвентаризация, а точечная доразведка)
═══════════════════════════════════════════════════════════════
Stage 1 дал полный список зависимостей (§1.7), но не определил их характер
доступа (только для чтения / для записи / статическая ссылка / нужен
геттер). Это нужно решить здесь, один раз, перед тем как проектировать
конструктор.

1.1 Для КАЖДОЙ строки таблицы §1.7 отчёта Stage 1 определить по факту (чтение
    тела метода, не предположение): используется ли она в 8 переносимых
    методах на ЧТЕНИЕ, на ЗАПИСЬ (присваивание), или вызывается как метод
    (callable-действие)? Построить таблицу: имя → тип доступа → как решается
    (getter-колбэк / setter-колбэк / прямой вызов callable / статическая
    ссылка, переданная один раз).

1.2 Три конкретных вопроса, определяющих архитектуру геттеров (проверить
    через grep по всему Supervertaler.py, не предполагать):
    - self.table — присваивается один раз (в __init__/setup) и переиспользу-
      ется, или пересоздаётся при открытии нового проекта/файла? Если
      пересоздаётся — UndoManager обязан получать его через геттер
      (get_table), не статическую ссылку, иначе после смены проекта будет
      держать ссылку на устаревший/уничтоженный виджет.
    - self.current_project — аналогично: меняется ли ссылка при
      открытии/создании нового проекта в течение сессии? (Очевидно да —
      иначе не было бы смысла в атрибуте — но подтвердить явно, что именно
      _apply_undo_redo_action/_apply_structural_history должны видеть АКТУ-
      АЛЬНОЕ значение на момент вызова, не значение на момент конструирова-
      ния UndoManager).
    - self._original_segment_order — где и когда присваивается? Читается
      ли ТОЛЬКО в _apply_structural_history, или где-то ещё тоже пишется в
      процессе (если пишется в другом месте — нужен getter; если это
      «общая корзина», которую сам _apply_structural_history и читает, и
      пишет — нужны и getter, и setter).
    По умолчанию: если атрибут может измениться после конструирования
    UndoManager — обязателен getter (и setter, если метод его модифицирует),
    НЕ статическая ссылка, переданная один раз. Раздельные getter/setter, не
    smart-property — по стилю, уже принятому в проекте (явные именованные
    колбэки).

1.3 project_modified — определить точный паттерн использования в телах 3
    методов, которые его трогают (_apply_undo_redo_action,
    _push_structural_undo, _apply_structural_history): это всегда
    `self.project_modified = True`, или где-то присваивается иное значение?
    Спроектировать колбэк по факту (скорее всего простой
    `mark_project_modified: Callable[[], None]`, но подтвердить).

1.4 Свести итоговую сигнатуру UndoManager.__init__ в таблицу: имя параметра
    → тип (Callable[[], X] для getter / Callable[[X], None] для setter /
    Callable[..., Y] для действия / прямая ссылка на объект) → откуда в
    SupervertalerQt.__init__ будет передан (например,
    get_current_project=lambda: self.current_project). Включить
    max_undo_levels как обычный параметр конструктора (default=100, сохраняя
    текущее поведение).

═══════════════════════════════════════════════════════════════
ЭТАП 2 — ПЕРЕНОС
═══════════════════════════════════════════════════════════════
1. Создать modules/undo_manager.py с классом UndoManager. __init__ принимает
   колбэки по таблице 1.4 плюс max_undo_levels; инициализирует
   self.undo_stack = [], self.redo_stack = [], self.max_undo_levels = ...
   внутри UndoManager (перенос состояния из бывших строк 6212–6214).
2. Перенести тела всех 8 методов в UndoManager. Рекомендация по стилю
   (снижает риск максимально): называть колбэки-геттеры ТАК, чтобы замена
   внутри тела метода была минимальной и прозрачной — например, там, где
   было `self.current_project`, если это geter-колбэк с именем
   `get_current_project`, тело метода меняется на
   `self.get_current_project()` (было — атрибут, стало — вызов). Явно
   пройтись по каждому телу и заменить обращения к бывшим self.-атрибутам/
   методам MainWindow на соответствующие self.<имя-колбэка> по таблице 1.4.
   Внутренние вызовы между самими 8 методами (11 MOVE→MOVE call sites из
   §1.6 отчёта Stage 1, например self._apply_undo_redo_action(...) внутри
   undo_action_handler) остаются как self.<имя>(...) без изменений — все 8
   методов живут в одном классе UndoManager, поэтому self по-прежнему
   разрешается корректно.
3. В SupervertalerQt.__init__ заменить строки 6212–6214 на создание
   self._undo_manager = UndoManager(<колбэки по таблице 1.4>).
4. На месте бывших тел 8 методов в SupervertalerQt оставить 8 делегатов с
   точно теми же именами и сигнатурами параметров, что были у оригиналов
   (не заменять на *args/**kwargs, если у оригинала были именованные
   параметры — сохранить их дословно для читаемости и чтобы сигнатура
   осталась самодокументируемой), тело — однострочный проброс в
   self._undo_manager.<то же имя>(...).
5. Убедиться, что все 28 self.-стиль call sites (из методов, остающихся в
   монолите — по списку §1.6 отчёта Stage 1) не редактировались.
6. Убедиться, что все 4 внешних duck-typed call sites (EditableGridTextEditor
   ×3 линии, modules/pseudo_translate_dialog.py:264) не редактировались.
7. Убедиться, что все 14 NOT MOVE методов остались нетронуты (см. Этап 3.4).

═══════════════════════════════════════════════════════════════
ЭТАП 3 — ВАЛИДАЦИЯ
═══════════════════════════════════════════════════════════════
1. py_compile Supervertaler.py + modules/undo_manager.py — чисто.
2. Сравнить 8 метрик с baseline_counts_batch5stage2.txt.
3. SHA256 всех файлов, кроме Supervertaler.py и нового
   modules/undo_manager.py, — совпадают с manifest_before_batch5stage2.txt.
4. Байт-в-байт сверка всех 14 NOT MOVE методов с
   not_moved_methods_batch5_snapshot.txt — SHA256 до/после, явный
   PASS/FAIL по каждому из 14.
5. ФУНКЦИОНАЛЬНЫЕ СЦЕНАРИИ (обязательны все — undo/redo это весь смысл
   модуля, здесь нельзя ограничиться «запустилось и не упало»):
   - Обычное редактирование сегмента → Ctrl+Z отменяет → Ctrl+Y/Ctrl+Shift+Z
     повторяет.
   - Пакетная операция (clear selected translations ИЛИ copy source to
     target bulk) → один Undo откатывает весь пакет целиком одним действием.
   - Split сегмента → Undo корректно восстанавливает исходный сегмент
     (проверяет и _apply_structural_history, и колбэки на
     _select_grid_row_by_id/_split_segment_grid_fast, которые сами не
     переносились, но вызываются из перенесённого кода).
   - Merge сегментов → аналогично, Undo восстанавливает.
   - Delete сегмента(ов) → Undo восстанавливает.
   - Кнопки/пункты меню Undo и Redo корректно активны/неактивны в
     зависимости от состояния стека (update_undo_redo_actions).
   - Ctrl+Shift+S (copy source to target) поверх заполненного перевода →
     Undo восстанавливает предыдущий текст (это внешний duck-typed вызов из
     EditableGridTextEditor._copy_source_to_target — самый рискованный
     сценарий на молчаливую регрессию, проверить обязательно).
   - Псевдоперевод (pseudo-translate) пакета сегментов → Undo отменяет пакет
     (второй duck-typed внешний вызов, modules/pseudo_translate_dialog.py).
   - Ctrl+Z / Ctrl+Shift+Z, нажатые КОГДА ФОКУС В ЯЧЕЙКЕ ГРИДА (не через
     меню) — путь EditableGridTextEditor.keyPressEvent, отдельно от обычного
     вызова через меню.
   - Много действий подряд (больше max_undo_levels) — старые записи стека
     корректно отбрасываются, приложение не падает и не тормозит.
6. Обычный smoke-тест (создание проекта → импорт → правка → сохранение →
   перезапуск → загрузка → проверка состояния).

═══════════════════════════════════════════════════════════════
ЭТАП 4 — ОТЧЁТ
═══════════════════════════════════════════════════════════════
Стандартный формат (как в Batch #4): diff stat, counts diff, SHA256 diff,
14×NOT MOVE byte-identity PASS/FAIL, таблица 1.4 (итоговая сигнатура
конструктора) как документация архитектуры, чеклист функциональных
сценариев (Этап 3.5) пункт за пунктом, результат smoke-теста, «Непокрытые
проверки» (если что-то не удалось прогнать — для VALIDATION_BACKLOG.md),
явный GO/NO-GO, один коммит: "Extract undo/redo manager into
modules/undo_manager.py (Batch #5 Stage 2, Step 5 of EXTRACTION_PLAN.md)".

Если контекст сессии к началу Этапа 3 (валидация) заметно разросся —
допустимо использовать /compact (или для Cline /smol) перед функциональными
сценариями, не теряя фактов Этапов 0–2 (они уже зафиксированы в файлах
manifest/baseline/snapshot — сессии есть на что опереться после сжатия).

СТОП-УСЛОВИЯ:
1. Этап 0.2 или 0.3 находит расхождение с зафиксированными Stage 1 фактами —
   остановиться, не пытаться угадать причину, сообщить владельцу.
2. Любой из 14 NOT MOVE методов после переноса не совпадает по SHA256 —
   немедленный откат (git revert), не пытаться «починить» точечно.
3. Функциональный сценарий Split/Merge/Delete → Undo не проходит — это
   самый рискованный путь (единственный, где перенесённый код напрямую
   зависит от 3 колбэков к grid-хелперам, которые сами не переносились) —
   остановиться, откатить, не пытаться чинить на лету.
4. После Этапа 4 — остановиться, ждать подтверждения владельца перед Step 6
   (Grid: helpers/pagination/filters, будущий Batch #6). Не продолжать
   автоматически.
