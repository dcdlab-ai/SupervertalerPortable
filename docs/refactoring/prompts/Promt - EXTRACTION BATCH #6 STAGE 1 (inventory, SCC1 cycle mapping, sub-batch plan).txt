РОЛЬ: Ты выполняешь ТОЛЬКО инвентаризацию и анализ в проекте Supervertaler.
Режим: READ-ONLY ANALYSIS. Ни одного изменения кода в этом этапе. Если
что-то покажется "заодно поправить" — не делать, зафиксировать как отдельное
наблюдение в отчёте.

ПРЕДПОСЫЛКА: Batch #5 (Step 5, Undo-менеджер) подтверждён GO — независимой
проверкой координатора и владельцем на живом приложении (Ctrl+Z на гриде
работает). Продолжаем от текущего HEAD (коммит "Extract undo/redo manager
into modules/undo_manager.py (Batch #5 Stage 2, Step 5 of
EXTRACTION_PLAN.md)").

ПОЧЕМУ ЭТОТ БАТЧ СНОВА НАЧИНАЕТСЯ С ОТДЕЛЬНОГО READ-ONLY ЭТАПА (и почему он
крупнее, чем Batch #5 Stage 1): Step 6 — это ~45 методов (не 8, как в Step 5)
на ТРИ файла назначения (`modules/grid/helpers.py`, `pagination.py`,
`filters.py`), а не один. Хуже того, план прямо предупреждает про
циклическую зависимость: "Пагинация ↔ populate (SCC#1) — на этом шаге НЕ
трогать populate/render; helpers получают всё параметрами, цикл не
переносится". Дальнейшая проверка (координатором, до этого промпта) показала:
- Диапазоны строк в EXTRACTION_PLAN.md для Step 6 (31574–32100,
  32150–32188, 55494–57100) устарели — считались до сдвига в −531 строку от
  батчей #4/#5.
- В этих окнах (с поправкой на сдвиг) фактически лежит ОКОЛО 87 методов —
  заметно больше ожидаемых ~45. Остальное — посторонний код (termbase-поиск,
  proofreading-заметки и т.п.), НЕ относящийся к Step 6.
- CODE_MAP_REFACTOR.md НЕ содержит готового поимённого списка 45 методов
  (секция "§16 Extraction Candidates" — это сводная таблица без деталей, не
  перечень). Список нужно построить с нуля через AST + чтение кода.
- Вероятный центральный метод цикла — `load_segments_to_grid` (строка ~41428
  на момент написания этого промпта, ПРОВЕРИТЬ через AST, не доверять этому
  числу) — уже фигурировал в Step 5 как колбэк-зависимость
  `_apply_structural_history`. Это ПОДТВЕРЖДАЕТ, что метод активно
  используется и грид-логикой, и структурными операциями — типичный признак
  участника цикла.

Поэтому Stage 1 здесь — не только инвентаризация состава, но и (а) явное
картирование цикла SCC#1 на границе с кандидатами Step 6, и (б) рекомендация,
на сколько под-батчей резать сам перенос (Stage 2+), потому что ~45 методов
одним коммитом — сам по себе неприемлемый по размеру и риску батч.

СКОУП STAGE 1 (и ничего больше): собрать факты о Step 6 EXTRACTION_PLAN.md и
выдать отчёт с (1) точным составом методов по трём файлам назначения, (2)
картой пересечения с циклом SCC#1 и рекомендацией, как его разорвать для
каждого затронутого метода, (3) рекомендацией по разбивке Stage 2+ на
под-батчи. Ноль изменений кода.

ЖЁСТКИЕ ЗАПРЕТЫ (для Stage 1):
- Не менять ни одной строки кода нигде в репозитории.
- Не создавать modules/grid/ или файлы-заглушки в нём.
- Не принимать архитектурное решение самому (как передавать
  table/project/populate-колбэк — через параметры метода, через konstruktor
  класса-контейнера, или иначе) — собрать факты, дать обоснованную
  рекомендацию, финальное решение за владельцем.
- Не пытаться "заодно" разорвать сам цикл SCC#1 (например, предлагая
  вынести load_segments_to_grid) — это прямо запрещено планом на этом шаге
  ("populate/render не трогать"). Задача — изолировать Step-6-методы ОТ
  цикла параметрами/колбэками, не трогая сам цикл.

═══════════════════════════════════════════════════════════════
ЗАДАЧИ STAGE 1
═══════════════════════════════════════════════════════════════

1.1 ПОЛНАЯ AST-ИНВЕНТАРИЗАЦИЯ ТРЁХ ОКОН + ИМЕНОВАННЫХ ХЕЛПЕРОВ
- Через ast.parse/lineno/end_lineno перечислить ВСЕ методы класса
  SupervertalerQt в трёх окнах (с запасом по краям от заявленных границ,
  учитывая что реальные границы сдвинуты примерно на −531 от значений
  EXTRACTION_PLAN.md, но проверить фактически, не считать -531 точным):
  окно A (пагинация, заявлено 31574–32100), окно B (фильтры/невидимые,
  заявлено 32150–32188), окно C (фильтры/невидимые продолжение, заявлено
  55494–57100).
- Отдельно найти точные текущие AST-границы 8 именованных хелперов из плана:
  _widget_is_alive, _segment_for_grid_row, _find_row_for_segment,
  _find_row_for_segment_id, _select_grid_row_by_id, _recompute_list_numbers,
  _reindex_grid_rows_from, _get_line_edit_text (координатор нашёл
  предварительно грепом по имени — 6 из 8 не в трёх основных окнах, а рядом
  с бывшей undo-зоной ~9400–9950 и в районе 28800/54200/57700 — ПРОВЕРИТЬ
  самостоятельно, не доверять этим числам дословно, они могли устареть уже к
  моменту выполнения этого промпта).
- Для КАЖДОГО метода в трёх окнах — определить по СОДЕРЖАНИЮ (не по
  расположению): относится ли он к пагинации / фильтрам / общим
  grid-хелперам / НЕ относится к Step 6 вообще (посторонний код в том же
  диапазоне, как _search_termbase_in_memory и подобные, обнаруженные
  координатором в окне A).
- Итог: полная таблица метод → границы → категория (PAGINATION / FILTERS /
  HELPERS / NOT STEP 6) → предлагаемый файл назначения.

1.2 ТОЧНЫЙ ИТОГОВЫЙ СОСТАВ
- Дать окончательный подтверждённый список методов на перенос по каждому из
  трёх файлов (modules/grid/helpers.py, pagination.py, filters.py). Сравнить
  итоговое число с ожиданием плана "~45" — объяснить расхождение, если оно
  есть (как было с "7 vs 8" в Step 5 — расхождения с документами это норма,
  не повод сомневаться в собственном AST-анализе).

1.3 КАРТА ЦИКЛА SCC#1 НА ГРАНИЦЕ С КАНДИДАТАМИ STEP 6
- Подтвердить фактически (через AST/grep), что "populate"-метод, вокруг
  которого возникает цикл — это load_segments_to_grid (или другой, если
  найдётся более подходящий кандидат) — найти точное текущее определение.
- Для КАЖДОГО метода из итогового списка 1.2 проверить: вызывает ли он
  напрямую load_segments_to_grid (или другой участник цикла — определить
  полный список участников цикла, если возможно установить его по вызовам,
  не обязательно все 15 методов SCC#1 целиком, но как минимум те, с кем
  реально соприкасаются кандидаты Step 6)?
- Для каждого найденного пересечения — предложить способ разрыва
  ИМЕННО СО СТОРОНЫ переносимого метода (переносимый метод получает нужное
  как параметр/колбэк при вызове, а не вызывает self.load_segments_to_grid
  напрямую) — НЕ предлагать трогать сам load_segments_to_grid или другие
  участники цикла.
- Если пересечений с циклом у какого-то метода НЕТ — явно пометить как
  "цикл не касается", это тоже важный факт для отчёта.

1.4 CALL SITES
- Для итогового списка 1.2 (ожидается заметно больше, чем 43 в Step 5, раз
  методов в 5+ раз больше) — grep/AST по всему репозиторию на все вызовы.
  Для каждого: self.-стиль изнутри методов, которые сами переносятся
  (внутренние, останутся рабочими автоматически при вербатим-переносе) / из
  методов, которые остаются в монолите (потребуют либо делегатов, либо
  замены — architecture-вопрос, не решать самому) / внешние duck-typed (как
  4 штуки в Step 5) / из UI-обработчиков событий (connect на кнопки
  пагинации/фильтров — отдельная категория, специфичная для этого шага).
- Явно посчитать итоговое число call sites каждой категории.

1.5 ЗАВИСИМОСТИ / КОЛБЭКИ (аналог таблицы §1.7 из Step 5, здесь наверняка
    больше зависимостей из-за втрое большего числа файлов назначения)
- Построить таблицу: метод → какие атрибуты/методы MainWindow использует
  (self.table, self.current_project и т.п. — план явно упоминает оба, но
  список наверняка шире, как в Step 5 оказалось с current_sort). Не
  ограничиваться подсказками плана — искать по факту в каждом теле.
- Отдельно отметить: какие зависимости общие сразу для НЕСКОЛЬКИХ из трёх
  файлов назначения (helpers.py вероятно нужен и pagination.py, и
  filters.py — если так, это влияет на архитектуру: возможно, helpers.py
  должен быть отдельным слоем, на который опираются два других, а не тремя
  независимыми файлами).

1.6 DYNAMIC_ENTRY_POINT / СТРОКОВЫЕ ССЫЛКИ
- Проверить весь итоговый список 1.2 на DYNAMIC_ENTRY_POINT в
  DEAD_CODE_REPORT.md (как это было с undo_action_handler/redo_action_handler
  в Step 5) — если найдутся такие методы, это сразу определяет необходимость
  делегатов-обёрток для них, аналогично Step 5.

1.7 РЕКОМЕНДАЦИЯ ПО АРХИТЕКТУРЕ (не решение агента)
На основе 1.3 (разрыв цикла) и 1.5 (общие зависимости между тремя файлами) —
предложить: функции с явными параметрами (table, project, колбэки) в каждом
файле — или класс-контейнер на файл (как UndoManager) — или один общий
класс/слой для всех трёх (если 1.5 покажет, что зависимости в основном
общие)? Дать обоснование, не просто взять шаблон Step 5 по умолчанию — здесь
архитектура может отличаться из-за трёх файлов вместо одного.

1.8 РЕКОМЕНДАЦИЯ ПО РАЗБИВКЕ STAGE 2+ НА ПОД-БАТЧИ (обязательный вывод)
~45 (или сколько получится по факту 1.2) методов одним коммитом — неприемлемо
по размеру/риску (сравни: Batch #5 Stage 2 переносил 8 методов и это уже был
непростой батч с 43 call sites). Предложить разбивку, скорее всего по файлу
назначения (аналог 3a/3b/3c для Step 3): например Stage 2 = helpers.py (если
на нём не завязаны два других), Stage 3 = pagination.py, Stage 4 =
filters.py — или иной порядок, если находки 1.5 подскажут другую логику
(например, helpers.py первым, потому что pagination.py/filters.py от него
зависят). Дать конкретный предлагаемый порядок и обоснование, с оценкой
количества методов/call sites на под-батч.

═══════════════════════════════════════════════════════════════
СТОП-УСЛОВИЕ STAGE 1
═══════════════════════════════════════════════════════════════
После завершения 1.1–1.8 — ОСТАНОВИТЬСЯ. Не создавать modules/grid/, не
трогать Supervertaler.py. Собрать отчёт (формат ниже) и ждать подтверждения
владельца по архитектуре, итоговому составу методов и предложенной разбивке
на под-батчи. Каждый под-батч Stage 2+ будет отдельным самодостаточным
промптом, подготовленным ПОСЛЕ этого решения.

═══════════════════════════════════════════════════════════════
ФОРМАТ ОТЧЁТА STAGE 1
═══════════════════════════════════════════════════════════════
- Полная таблица 1.1/1.2 (метод → границы → категория → файл назначения),
  сгруппированная по трём файлам назначения.
- Итоговые числа vs ожидание плана "~45" — с объяснением расхождения.
- Карта цикла SCC#1 (1.3): подтверждённый центральный метод, список
  найденных пересечений, предложенный способ разрыва для каждого.
- Полная разбивка call sites по категориям (1.4) с итоговыми числами.
- Таблица зависимостей (1.5), включая пересечения между тремя файлами
  назначения.
- Результат проверки DYNAMIC_ENTRY_POINT (1.6) — список, если есть.
- Рекомендация по архитектуре (1.7) с обоснованием.
- Рекомендация по разбивке Stage 2+ на под-батчи (1.8) — конкретный порядок,
  обоснование, оценка размера каждого.
- Явный вывод: "ГОТОВ К STAGE 2" или "ЕСТЬ БЛОКЕРЫ/ОТКРЫТЫЕ ВОПРОСЫ" — со
  списком, что требует решения владельца.
- Подтверждение: ни одна строка кода не изменена.

Если контекст сессии разрастётся до завершения всех пунктов 1.1–1.8 —
допустимо использовать /compact (Zcode) или /smol (Cline) в процессе, не
теряя уже собранных таблиц (сохранять промежуточные находки в
docs/refactoring/audits/ по ходу работы, а не держать только в контексте
диалога).
