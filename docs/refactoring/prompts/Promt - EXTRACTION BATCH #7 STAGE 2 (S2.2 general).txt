ПЕРЕНОС S2.2 — Settings Service, general + clipboard reading (Batch #7 Stage 2, под-батч 2/5)

РОЛЬ: Ты выполняешь перенос кода (не разведка, не аудит). Разведка границ
уже сделана Б2 (дважды — первый прогон был на устаревшем HEAD и не
используется, второй, на origin/main после fetch, ниже).

Этап 0 — ОБЯЗАТЕЛЬНО перед всем остальным:
  git fetch origin
  git merge-base --is-ancestor 3e5c100c HEAD && echo OK || echo STOP
  Если STOP — git checkout origin/main (или pull) и повторить проверку.
  Не читать и не трогать код, пока не OK.

Этап 1 — базовая линия:
- git rev-parse HEAD; если отличается от 3e5c100c0c1a5a5dff9a2e74e74ec9cb4c0ece9e —
  указать новый SHA (ожидается потомок, не расхождение) и продолжить.
- SHA256-манифест всех .py, py_compile, 8 метрик — зафиксировать как обычно.

Этап 2 — перенос (3 метода переезжают в modules/settings_service.py,
1 метод — NOT-MOVE):

ПЕРЕНОСЯТСЯ (тонкий делегат на SupervertalerQt для каждого, форвардящий на
self.settings_service):
  _load_general_settings_from_file   44897–44936 (40 строк)
  load_clipboard_privacy_settings    44704–44713 (10 строк)
  save_general_settings               44938–44943 (6 строк)

Перед переносом каждого — проверить тело на зависимость от self.<qt-атрибут>
кроме settings_dir/log (как в S2.1). Если найдётся что-то ещё — не изобретать
новый параметр конструктора самостоятельно, остановиться и доложить
координатору, что именно нужно.

Делегаты обязательны (не опционально) — конкретные потребители:
  _load_general_settings_from_file: 3 других метода SupervertalerQt (строки
    41560, 44315, 44507 — определить и назвать в отчёте, какие это методы)
    + сам load_general_settings (см. ниже, NOT-MOVE, продолжит работать через
    делегат без изменений в своём теле)
  load_clipboard_privacy_settings: внутренний вызов 23488 (SupervertalerQt) +
    modules/clipboard_manager_widget.py:771 (getattr(self._parent_app,
    'load_clipboard_privacy_settings', None) — не менялся с отчёта Stage 1)
  save_general_settings: 24 внутренних вызова SupervertalerQt + 3 в
    SuperlookupTab через mw/self.main_window (один корректно защищён и
    вызван с аргументом; два — 66780 и 66823 — БАГ (вызов без обязательного
    аргумента settings, guard проверяет не тот атрибут). Делегат обязан
    сохранить ТОЧНО ТУ ЖЕ сигнатуру (обязательный позиционный аргумент, без
    default) — цель НЕ чинить баг, а сохранить его поведение бит-в-бит
    (TypeError при срабатывании этого пути, как и раньше)

NOT-MOVE (явно, не забыть в отчёте):
  load_general_settings 44542–44638 (97 строк) — код НЕ трогать вообще.
  Его собственный внутренний вызов self._load_general_settings_from_file()
  (строка 44545) продолжит работать через делегат выше без изменений тела
  метода. Это и есть выполнение V3-требования SPLIT — отдельного разделения
  тела на IO/не-IO часть не делать, оно не нужно.

Этап 3 — валидация (Step 7 конвенция):
- Живой прогон приложения; сохранение/загрузка General Settings и
  Clipboard privacy settings через UI.
- getattr-guarded путь modules/clipboard_manager_widget.py:771 — должен
  реально находить метод, не проваливаться в None.
- Явно попытаться воспроизвести (не чинить) баг save_general_settings без
  аргумента — если легко доступен из UI/по логам; если недоступен без
  спец. состояния — не тратить время, зафиксировать как N-пункт в отчёте
  для VALIDATION_BACKLOG (пред-существующий, не из S2.2).
- py_compile, SHA256-манифест (кроме settings_service.py и Supervertaler.py),
  8 метрик, смоук-тест.
- Раздел "Непокрытые проверки" — обязателен.

ФОРМАТ ОТЧЁТА: как в S2.1 (структура + NOT-MOVE + Непокрытые проверки +
подтверждение отсутствия правок кода вне заявленного диффа).
