РОЛЬ: Ты выполняешь механический перенос кода в проекте Supervertaler.
Режим: ACTIVE REFACTOR. Факты ниже — новые находки координатора ПОСЛЕ отчёта
Stage 1 (Stage 1 не давал точных границ для filters.py, только оценку
диапазона и состав). Короткая AST-спот-проверка в начале всё равно
обязательна.

ПРЕДПОСЫЛКА: Batch #6 Stage 3 (pagination.py) подтверждён GO — отчётом
агента (все 3 стоп-условия чисты, 56/56 сценариев, Undo/Redo 38/38) И
владельцем на живом приложении (клавиатура/клики пагинации, on_cell_clicked,
метка активного текстового фильтра, визуальных проблем нет). Один пункт из
«Непокрытых проверок» Stage 3 — ветка `_ImportProgressDialog` при
rows_to_populate ≥ 200 — не был лично проверен владельцем на реальном
запуске ПОСЛЕ переноса; заведён отдельно в VALIDATION_BACKLOG.md (пункт 9,
статус OPEN, риск LOW — тот же путь уже проверялся на 401 сегменте в Batch
#3b п.3, а сам вызов перенесён 1:1) и НЕ блокирует Stage 4. Продолжаем от
HEAD (коммит "Extract grid pagination into modules/grid/pagination.py
(Batch #6 Stage 3, Step 6 of EXTRACTION_PLAN.md)"). Обязательно прочитать
docs/refactoring/reports/ОТЧЁТ Batch #6 Stage 1.txt (общий контекст, карта
цикла SCC#1, состав filters.py) и docs/refactoring/reports/ОТЧЁТ Batch #6
Stage 3.txt (образец качества/формата; АРХИТЕКТУРНОЕ РЕШЕНИЕ, принятое там —
функции с явными параметрами, а не класс — продолжается и здесь, см. СКОУП).

ЭТО ПОСЛЕДНИЙ ИЗ ТРЁХ ПОД-БАТЧЕЙ STEP 6: Stage 2 = helpers.py (готово) →
Stage 3 = pagination.py (готово) → Stage 4 = filters.py (этот промпт).
Этот коммит ЗАКРЫВАЕТ Step 6 EXTRACTION_PLAN.md целиком.

ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ О МЕТОДОЛОГИИ (отличается от Stage 3): в отличие от
pagination.py, кластер filters.py — НЕ один непрерывный блок. Координатор
пересчитал AST на HEAD (коммит Stage 3) и обнаружил, что 20 методов состава
разбросаны в окне 51794–55821 (~4028 строк), из которых filters.py
принадлежит ТОЛЬКО ~1097 строк. Между нужными методами лежат ЦЕЛИКОМ
посторонние блоки: (а) `show_manage_views_dialog` 52202–52369 (168 строк) —
физически ВНУТРИ окна, между `_update_file_filter_combo` и
`toggle_invisible_display`, но НЕ в составе (открытый вопрос Stage 1 §8 п.3,
остаётся нерешённым, не в скоупе); (б) `_refresh_source_column_display`
52555–52568 (14 строк, Step 8/теги) — сразу после `refresh_grid_invisibles`;
(в) огромный блок spellcheck/словарей 52645–53126 (~480 строк, ~12 методов);
(г) огромный блок комментариев к сегментам + voice dictation 53510–55753
(~2240 строк, ~45 методов). Диапазон «с такой-то по такую-то строку» здесь
НЕ РАБОТАЕТ — переносить строго 20 методов ПО ИМЕНИ из списка ниже, как
ПРЯМЫЕ методы класса SupervertalerQt (node.body, не ast.walk по дереву), и
явно исключать (а)–(г), даже вплотную прилегающие к нужным методам.

СКОУП STAGE 4 (и ничего больше): перенести 20 методов в НОВЫЙ файл
modules/grid/filters.py. Архитектура — ПО ПРЕЦЕДЕНТУ Stage 2/3: функции с
явными параметрами, НЕ класс (на Stage 3 владелец отклонил рекомендацию
Stage 1 §6 про класс-контейнер в пользу функций; тот же принцип продолжается
здесь, если явно не будет решено иначе). ОГОВОРКА: у нескольких методов
(особенно `clear_filters` — 124 строки, `apply_advanced_filters` — 77 строк,
`apply_sort` — 146 строк) уже сейчас много self-специфичных чтений/записей
(см. находки Б/Ж/З ниже) — если сигнатура чистой функции выйдет за разумные
пределы (по аналогии с `apply_pagination_to_grid` в Stage 3, где вышло ~15
параметров), РАЗРЕШАЕТСЯ сгруппировать НЕИЗМЕНЯЕМЫЕ pass-through параметры в
лёгкий dataclass/NamedTuple ТОЛЬКО ради удобства сигнатуры (не хранит
состояние между вызовами, НЕ аналог класса-контейнера из Stage 1 §6) —
явно задокументировать это решение в отчёте, если применено.

СОСТАВ (20 методов, координатор пересчитал AST на HEAD Stage 3, ВСЕ — ПРЯМЫЕ
методы класса SupervertalerQt — ПЕРЕПРОВЕРИТЬ самостоятельно тем же
способом обязательно, см. ПРЕДУПРЕЖДЕНИЕ выше):

_highlight_text_in_widget 51794–51833 (40, используется ТОЛЬКО из
apply_filters, 2 вызова — 0 внешних call sites подтверждено grep'ом
координатора по всему репозиторию),
_clear_filter_highlights_in_widget 51835–51873 (39),
apply_filters 51875–51967 (93),
clear_filters 51969–52092 (124),
_on_file_filter_changed 52094–52157 (64),
_update_file_filter_combo 52159–52200 (42),
[ИСКЛЮЧЕНО: show_manage_views_dialog 52202–52369 (168) — НЕ переносится],
toggle_invisible_display 52371–52399 (29),
toggle_all_invisibles 52401–52440 (40),
refresh_grid_invisibles 52442–52553 (112),
[ИСКЛЮЧЕНО: _refresh_source_column_display 52555–52568 (14) — Step 8/теги,
НЕ переносится],
apply_invisible_replacements 52570–52603 (34),
reverse_invisible_replacements 52605–52644 (40),
[ИСКЛЮЧЁН ЦЕЛЫЙ БЛОК 52645–53126, ~480 строк, ~12 методов — spellcheck/
словари, полностью посторонний],
filter_empty_segments 53128–53170 (43),
_clear_all_filter_highlights 53172–53182 (11),
apply_quick_filter 53184–53245 (62),
show_advanced_filters_dialog 53275–53284 (10),
apply_advanced_filters 53286–53362 (77),
apply_sort 53364–53509 (146),
[ИСКЛЮЧЁН ЦЕЛЫЙ БЛОК 53510–55753, ~2240 строк, ~45 методов — комментарии
сегментов, voice dictation, полностью посторонний],
filter_on_selected_text 55759–55821 (63).

Отдельно, физически рядом с БЫВШИМ блоком пагинации (не в окне выше, сразу
после делегата _get_line_edit_text из helpers.py, перед посторонним
_build_warning_banner): _ensure_shared_filter 28544–28557 (14),
_ensure_primary_filters_ready 28559–28572 (14).

ИТОГО: 20 методов, 1097 строк — день-в-день совпадает с оценкой Stage 1 §1;
подтверждает, что ни один из 20 не был задет Stage 2/3.

═══════════════════════════════════════════════════════════════
ДОПОЛНИТЕЛЬНЫЕ НАХОДКИ КООРДИНАТОРА (landmines, требуют решения на Этапе 1)
═══════════════════════════════════════════════════════════════

А) SCC#1 / apply_sort — ЕДИНСТВЕННОЕ пересечение состава с циклом (Stage 1
   §2, пересечение 1). Тело apply_sort (текущие строки ~53409 и ~53500,
   ПЕРЕПРОВЕРИТЬ фактически) содержит 2 прямых вызова self.load_segments_
   to_grid() в ветке «полная перезагрузка». РЕШЕНИЕ (наследуется от Stage 1
   §7/§8 вопрос 6, не обсуждается): делегат apply_sort в монолите передаёт
   reload_callback=self.load_segments_to_grid; pure-функция вызывает
   reload_callback() вместо self.load_segments_to_grid() в обоих местах.
   Сам load_segments_to_grid НЕ трогается. apply_sort также использует
   self.tr(...) один раз и self.grid_page_size дважды (читает) — та же
   tr-колбэк-логика, что в Stage 3 пункт Г (_update_pagination_ui), плюс
   grid_page_size обычным параметром.

Б) apply_invisible_replacements — ВНЕШНИЕ вызыватели (координатор
   перепроверил repo-wide grep, список Stage 1 §3 неполный по этому пункту):
   (1) modules/undo_manager.py — bound-method callback, инжектируется при
   конструировании UndoManager (известно из Batch #5); (2) НОВАЯ находка —
   modules/pseudo_translate_dialog.py:250-251:
   `mw.apply_invisible_replacements(new_target) if hasattr(mw,
   "apply_invisible_replacements") else new_target` — hasattr-guarded прямой
   вызов метода главного окна. Делегат В МОНОЛИТЕ обязан сохранить ИМЕННО
   ЭТО имя (принимать один позиционный аргумент — target-текст, возвращать
   строку) — иначе оба внешних пути сломаются молча (hasattr просто не
   найдёт метод; undo_manager получит None при конструировании).

В) filter_on_selected_text — DYNAMIC_ENTRY_POINT (DEAD_CODE_REPORT.md,
   подтверждено Stage 1 §5: create_shortcut:7216, Ctrl+Shift+F). Координатор
   дополнительно нашёл источник конфигурации: modules/shortcut_manager.py
   содержит ДВЕ записи ("filter_selected_text" и скрытый дубликат
   "clear_filter") с `"action": "filter_on_selected_text"` — судя по всему,
   таблица, из которой строится диспетчер shortcut'ов (getattr(self,
   action) где-то в обработчике). Имя метода — часть строкового контракта,
   менять нельзя.

Г) _highlight_text_in_widget — используется ТОЛЬКО внутри apply_filters (2
   вызова), 0 внешних call sites (grep repo-wide подтверждён). Можно
   оформить как обычную функцию модуля filters.py (или вложенный helper) —
   отдельный колбэк не нужен.

Д) refresh_grid_invisibles читает/пишет self._suppress_target_change_
   handlers — тот же флаг, что уже фигурирует в apply_pagination_to_grid
   (pagination.py, Stage 3). Тот же self-специфичный getattr(...,False)/
   setattr-паттерн: остаётся в делегате, pure-функция получает текущее
   значение параметром и возвращает решение делегату.

Е) ИМПОРТ ИЗ helpers.py/pagination.py ВМЕСТО КОЛБЭКА (принцип Stage 3 пункт
   Д, продолжается): по Stage 1 §4 filters.py — верхний слой зависимостей
   (filters → helpers + pagination), обе уже перенесены. Как минимум
   `_is_text_filter_active` и `_get_line_edit_text` уже есть в
   modules/grid/helpers.py — использовать прямой импорт, а не колбэк, везде,
   где методы filters.py в них нуждаются. Проверить полный список экспортов
   modules/grid/__init__.py и не полагаться только на эти два примера —
   пройтись по ВСЕМ зависимостям 20 методов.

Ж) 4 метода (apply_filters, clear_filters, _on_file_filter_changed,
   apply_quick_filter) напрямую вызывают self._apply_pagination_to_grid(...).
   Это НЕ кандидат на прямой импорт (в отличие от Е): _apply_pagination_to_
   grid в монолите — сам делегат, собирающий ~15 self-специфичных
   параметров (виджеты, populate-колбэки, состояние) для чистой функции
   apply_pagination_to_grid из pagination.py; воссоздавать эту сборку ещё
   раз внутри filters.py избыточно и хрупко. РЕКОМЕНДАЦИЯ: передавать как
   колбэк-параметр (apply_pagination_callback=self._apply_pagination_to_grid,
   вызывается без аргументов — как в существующих call sites) — тот же
   приём, что для populate-колбэков в Stage 3.

З) clear_filters дополнительно читает/пишет self.grid_current_page напрямую
   (похоже, сброс страницы при очистке фильтра) — self-специфичный
   getattr/setattr в делегате, как и прочие подобные случаи.

═══════════════════════════════════════════════════════════════
ЖЁСТКИЕ ЗАПРЕТЫ
═══════════════════════════════════════════════════════════════
- Не переносить show_manage_views_dialog (52202–52369) — физически внутри
  окна, но вне состава; открытый вопрос Stage 1 §8 п.3 остаётся
  нерешённым и НЕ в скоупе этого батча.
- Не переносить _refresh_source_column_display (52555–52568) — Step 8/теги.
- Не трогать блок spellcheck/словарей (52645–53126) и блок
  комментариев/voice dictation (53510–55753) — полностью посторонние Step 6,
  даже вплотную прилегают к нужным методам.
- Не трогать load_segments_to_grid — только вызывать через reload_callback
  внутри apply_sort (пункт А).
- В filters.py — по умолчанию функции с явными параметрами (не класс), см.
  СКОУП про допустимую оговорку с dataclass для сигнатур.
- 20 делегатов в монолите — точные исходные имена/сигнатуры, ОСОБО для
  filter_on_selected_text и apply_invisible_replacements (пункты Б/В).
- Не переписывать call sites этих 20 методов за пределами их тел и
  создаваемых делегатов.
- Один шаг = один коммит.

═══════════════════════════════════════════════════════════════
ЭТАП 0 — БАЗОВАЯ ЛИНИЯ И СПОТ-ПРОВЕРКА
═══════════════════════════════════════════════════════════════
1. git status чист, продолжаем от указанного коммита Stage 3.
2. AST-проверка границ — ОБЯЗАТЕЛЬНО только среди прямых методов класса
   SupervertalerQt (см. ПРЕДУПРЕЖДЕНИЕ выше), сверить с таблицей выше.
   Дополнительно переисследовать ВСЁ окно 51780–55830 целиком (как Stage 3
   Этап 0.2) — подтвердить ИМЕННО эти 20 плюс ИМЕННО эти исключения,
   ничего лишнего не включено и не пропущено.
3. Снять SHA256-снимки show_manage_views_dialog и
   _refresh_source_column_display (аналог NOT-MOVE в Stage 3) — они
   физически внутри окна и наиболее рискованы на случайное включение.
4. Снять контрольный SHA256 блока 52645–53126 (spellcheck) и блока
   53510–55753 (comments/dictation) как единых текстовых диапазонов — для
   сверки после переноса, что внутри ничего не сдвинулось/не изменилось.
5. Подтвердить фактически (grep по ВСЕМУ репозиторию, не только монолиту):
   call sites из пунктов А/Б/В/Ж, плюс полный список внешних (non-monolith)
   вызывателей всех 20 методов.
6. SHA256 всех .py файлов → manifest_before_batch6stage4.txt (сейчас 150
   файлов).
7. py_compile чисто.
8. baseline_counts_batch6stage4.txt (8 метрик, та же команда, что в
   Stage 2/3).
9. Call sites всех 20 методов по всему репозиторию →
   callsites_before_batch6stage4.txt.

═══════════════════════════════════════════════════════════════
ЭТАП 1 — ФОКУС-PREFLIGHT
═══════════════════════════════════════════════════════════════
1.1 Решить/подтвердить пункт А (apply_sort: reload_callback + tr_callback +
    grid_page_size параметром).
1.2 Решить пункт Ж (apply_pagination_callback для 4 методов) — таблица
    метод → сигнатура.
1.3 Найти ВСЕ случаи пункта Е (что ещё из зависимостей 20 методов уже есть
    в helpers.py/pagination.py) — свериться с modules/grid/__init__.py.
1.4 Свести итоговую таблицу сигнатур (аналог Stage 3 §1.4) — эта таблица
    также де-факто закрывает документацию Step 6 целиком (все 3 файла
    modules/grid/), отразить в отчёте.
1.5 DYNAMIC_ENTRY_POINT — подтвердить фактически для всех 20 (минимум 4
    обязательных из Stage 1 §5: _on_file_filter_changed,
    toggle_all_invisibles, show_advanced_filters_dialog,
    filter_on_selected_text — плюс перепроверить остальные 16,
    DEAD_CODE_REPORT.md мог устареть). Делегаты для ВСЕХ 20 единообразно.
1.6 Оценить сигнатуры на предмет оговорки из СКОУП про dataclass/NamedTuple
    — принять решение и обосновать в отчёте.

═══════════════════════════════════════════════════════════════
ЭТАП 2 — ПЕРЕНОС
═══════════════════════════════════════════════════════════════
1. Создать modules/grid/filters.py. Расширить modules/grid/__init__.py
   (реэкспорт новых функций, сохранить существующий экспорт helpers.py и
   pagination.py без изменений).
2. Перенести 20 тел как pure-функции по сигнатурам из 1.4.
3. Импорт в Supervertaler.py: `from modules.grid import filters as
   grid_filters` (по образцу Stage 2/3).
4. 20 делегатов на месте бывших тел, точные исходные имена/сигнатуры,
   ОСОБО — filter_on_selected_text и apply_invisible_replacements.
5. Убедиться, что show_manage_views_dialog и _refresh_source_column_display
   не тронуты (сверка по снимку Этапа 0.3).
6. Убедиться, что блоки spellcheck (52645–53126) и comments/dictation
   (53510–55753) не тронуты (сверка по снимку Этапа 0.4).
7. После переноса — обновить PROJECT_STATUS.md и EXTRACTION_PLAN.md: Step 6
   закрывается ЦЕЛИКОМ (все 3 под-батча Stage 2/3/4 выполнены) — явно
   раскрыть отдельным пунктом отчёта Этапа 4 (по договорённости
   VALIDATION_BACKLOG.md о правках планировочных документов).

═══════════════════════════════════════════════════════════════
ЭТАП 3 — ВАЛИДАЦИЯ
═══════════════════════════════════════════════════════════════
1. py_compile Supervertaler.py + modules/grid/*.py — чисто.
2. 8 метрик vs baseline.
3. SHA256 всех файлов, кроме Supervertaler.py и filters.py (+ изменённого
   __init__.py), — совпадают с manifest_before.
4. Байт-в-байт/текстовое совпадение show_manage_views_dialog,
   _refresh_source_column_display, блока spellcheck, блока
   comments/dictation.
5. ФУНКЦИОНАЛЬНЫЕ СЦЕНАРИИ:
   - Quick filter (apply_quick_filter) на проекте с несколькими сегментами.
   - Advanced Filters (show_advanced_filters_dialog → apply_advanced_
     filters) — минимум 2 условия одновременно.
   - File filter (_on_file_filter_changed, _update_file_filter_combo) на
     многофайловом проекте.
   - Invisible characters (toggle_invisible_display, toggle_all_invisibles,
     refresh_grid_invisibles, apply_invisible_replacements,
     reverse_invisible_replacements) — цикл показать/скрыть, убедиться, что
     текст сегментов не портится.
   - Empty segments filter (filter_empty_segments).
   - Ctrl+Shift+F / filter_on_selected_text — toggle-поведение (наложить/
     снять фильтр по выделенному тексту), включая проверку, что shortcut
     реально диспетчеризируется по имени метода (пункт В).
   - Sort (apply_sort) — минимум один сценарий, реально проходящий через
     reload_callback (полная перезагрузка), не только «пересортировка на
     месте» — единственная прямая точка пересечения с циклом SCC#1 в этом
     батче.
   - clear_filters — проверить сброс grid_current_page (пункт З) и
     восстановление немодифицированного состояния грида.
   - Undo/Redo регрессия (Batch #5) — ПОВТОРИТЬ третий раз (split/merge/
     delete → Undo).
   - Пагинация (Stage 3) остаётся рабочей после переноса filters.py —
     минимум переход по страницам + смена page size, поскольку 4 метода из
     этого батча вызывают _apply_pagination_to_grid.
6. Обычный smoke-тест.

═══════════════════════════════════════════════════════════════
ЭТАП 4 — ОТЧЁТ
═══════════════════════════════════════════════════════════════
Стандартный формат (как Stage 2/3): diff stat, counts diff, SHA256 diff,
снимки НЕ-переносимых блоков PASS/FAIL, итоговая таблица сигнатур (закрывает
документацию всего modules/grid/), чеклист функциональных сценариев, ОСОБО —
Undo/Redo регрессия и SCC#1/apply_sort-сценарий, результат smoke,
«Непокрытые проверки», явный GO/NO-GO, явно отметить, что Step 6
EXTRACTION_PLAN.md закрывается целиком этим коммитом. Один коммит:
"Extract grid filters into modules/grid/filters.py (Batch #6 Stage 4, Step 6
of EXTRACTION_PLAN.md — closes Step 6)".

Если контекст сессии разрастётся — /compact (Zcode) или /smol (Cline) перед
Этапом 3.

СТОП-УСЛОВИЯ:
1. Этап 0.2 находит методы НЕ как прямые методы класса, либо состав/границы
   20 методов отличаются от заявленных — остановиться, не включать
   "находки" без подтверждения владельца.
2. show_manage_views_dialog / _refresh_source_column_display / блок
   spellcheck / блок comments-dictation не совпадают по SHA256/тексту после
   переноса — немедленный откат.
3. Undo/Redo регрессия (split/merge/delete → Undo) не проходит —
   немедленный откат, кросс-батчевая регрессия трёх предыдущих батчей (#5,
   #6 Stage 2, #6 Stage 3).
4. Сценарий apply_sort/reload_callback (реальная полная перезагрузка через
   сортировку) не проходит — откат, единственная точка пересечения с
   циклом SCC#1 в этом батче.
5. После Этапа 4 — остановиться. Step 6 закрыт целиком; ждать решения
   владельца по направлению дальше (следующий шаг EXTRACTION_PLAN.md). Не
   продолжать автоматически.
