РОЛЬ: Ты выполняешь ТОЛЬКО инвентаризацию и анализ в проекте Supervertaler.
Режим: READ-ONLY ANALYSIS. Ни одного изменения кода в этом этапе. Если
что-то покажется "заодно поправить" — не делать, зафиксировать как отдельное
наблюдение в отчёте.

ПРЕДПОСЫЛКА: Step 6 EXTRACTION_PLAN.md закрыт ЦЕЛИКОМ и подтверждён GO
(Batch #6 Stage 4, filters.py — независимой проверкой координатора и
владельцем на живом приложении). Продолжаем от HEAD (коммит "Extract grid
filters into modules/grid/filters.py (Batch #6 Stage 4, Step 6 of
EXTRACTION_PLAN.md — closes Step 6)"). Монолит сейчас 68 386 строк.

═══════════════════════════════════════════════════════════════
ЕСЛИ ПРОДОЛЖАЕШЬ ЧУЖУЮ НЕЗАВЕРШЁННУЮ РАБОТУ НА ЭТОМ ЭТАПЕ
═══════════════════════════════════════════════════════════════
Stage 1 здесь длиннее обычного (9 подзадач, разбросанные кластеры — см.
ниже) — не исключено, что придётся продолжать после чужой прерванной
сессии. Перед началом:
- открыть docs/refactoring/audits/step7_stage1_findings.md, если он уже
  существует — там отмечено, какие подзадачи (1.1–1.9 ниже) уже закрыты и
  какие таблицы/числа уже собраны;
- прочитать последний абзац «ПЕРЕДАЧА СМЕНЫ» в этом файле, если есть.
Продолжай с первой незакрытой подзадачи. Не пересматривай уже собранные
факты без явной причины. Если останавливаешься сам (лимит, конец сессии) —
запиши в этот файл, что закрыто, и добавь свою «ПЕРЕДАЧА СМЕНЫ» (5–10
строк: какие суждения принял по ходу, что осталось, есть ли сомнения).
Обновляй этот файл ПОСЛЕ КАЖДОЙ подзадачи (1.1, 1.2, ...), не одним махом
в конце.

═══════════════════════════════════════════════════════════════
ПОЧЕМУ ЭТОТ STAGE 1 ТРЕБУЕТ ОСОБОЙ ТЩАТЕЛЬНОСТИ
═══════════════════════════════════════════════════════════════
EXTRACTION_PLAN.md описывает Step 7 как "два окна" (48308–48750 + 29585–
30500) с ~25 методами. Оба числа устарели и, похоже, неверны по форме, не
только по смещению:

- Координатор (после Batch #6 Stage 4, на HEAD) прогнал предварительный
  read-only AST-грep и нашёл НЕ два окна, а МИНИМУМ 9 разрозненных
  кластеров по всему файлу, от строки ~32231 до ~61247 (span ~29000
  строк) — заметно более рассеяно, чем даже filters.py в Step 6 (где было
  4 посторонних блока внутри одного окна; здесь кластеры физически далеко
  друг от друга, с огромными посторонними участками между ними — grid,
  spellcheck-логика, comments/dictation и т.п., уже частично перенесённые
  в Step 6). "Два окна" из плана — не рабочая методология, ориентироваться
  на неё не надо.
- Предварительно найдено ~46 методов, ~1709 строк — почти вдвое больше
  плановых "~25" (тот же паттерн, что был с Step 6: план обещал ~45,
  реально нашлось ~87 в окне, из них ~48 по делу). Расхождение — норма,
  не повод сомневаться в собственном AST-анализе, но пересчитать заново
  обязательно.
- `load_general_settings` — координатор насчитал ~61 текстовых совпадений
  по всему репозиторию (включая определение) грепом на HEAD; план прямо
  предупреждает про fan-in "до 48" реальных call sites и, что важнее,
  явно требует сохранить СЕМАНТИКУ: метод перечитывает файл с диска
  КАЖДЫЙ раз, это не случайность, а осознанное поведение (кэшировать
  запрещено). Это главный риск-фактор Step 7, аналог SCC#1 в Step 6 —
  требует полного и точного картирования.
- Целевой модуль (`modules/config_manager.py`, класс `ConfigManager`,
  singleton-паттерн для user_data путей) — УЖЕ существует и УЖЕ является
  классом, не набором функций. Это меняет архитектурный вопрос по
  сравнению со Step 5/6 (там был чистый выбор "функции vs класс" и
  победили функции) — здесь вопрос другой: расширять ли существующий
  класс ConfigManager напрямую, обернуть ли его новым слоем, или сделать
  функции, использующие ConfigManager как зависимость. Не переносить
  автоматически решение Step 5/6 сюда без анализа.
- Две функции миграции (`_migrate_settings_to_unified`,
  `_migrate_to_workbench_layout`) — одноразовые, вызываются из
  конструктора/инициализации (минимум 2 call site у второй, координатор
  нашёл навскидку строки ~6327 и ~6659 — ПРОВЕРИТЬ). План явно требует
  оставить их в сервисе, не выносить отдельно и не удалять.

СКОУП STAGE 1 (и ничего больше): собрать факты о Step 7 EXTRACTION_PLAN.md
и выдать отчёт с (1) точным составом методов по кластерам, (2) картой
call sites с особым фокусом на fan-in load_general_settings и сохранение
семантики "чтение с диска при каждом вызове", (3) рекомендацией по
архитектуре (отношение к существующему ConfigManager), (4) рекомендацией
по разбивке Stage 2+ на под-батчи. Ноль изменений кода.

ЖЁСТКИЕ ЗАПРЕТЫ (для Stage 1):
- Не менять ни одной строки кода нигде в репозитории, включая
  modules/config_manager.py.
- Не создавать modules/settings_service.py или файлы-заглушки.
- Не принимать архитектурное решение самому (расширять ConfigManager /
  обёртка / функции-с-зависимостью) — собрать факты, дать обоснованную
  рекомендацию, финальное решение за владельцем.
- Не предлагать "заодно оптимизировать" load_general_settings (например,
  добавить кэширование) — это прямо противоречит требованию плана
  сохранить текущую семантику. Задача Stage 1 — зафиксировать поведение
  как есть, не улучшать его.

═══════════════════════════════════════════════════════════════
ЗАДАЧИ STAGE 1
═══════════════════════════════════════════════════════════════

1.1 ПОЛНАЯ AST-ИНВЕНТАРИЗАЦИЯ ПО КЛАСТЕРАМ
Через ast.parse/lineno/end_lineno, ТОЛЬКО прямые методы класса
SupervertalerQt (node.body класса, НЕ ast.walk). Предварительные координаты
координатора (ПРОВЕРИТЬ каждую, не доверять дословно — это грep "на глаз",
не AST):
  _get_settings_dir ~44639–44641, _get_unified_settings_path ~44643–44645,
  _load_unified_settings ~44647–44661, _save_unified_settings ~44663–44672,
  _load_settings_section ~44674–44676, _save_settings_section ~44678–44682,
  load_general_settings ~44522–44618, load/save clipboard privacy
  ~44690–44722, _migrate_settings_to_unified ~44724–44814,
  _migrate_to_workbench_layout ~44816–?(конец не проверен), general-from-file
  ~44883–44929, dictation ~44931–45002, language pair/language ~45004–45021
  и ~45228–45261, voice vocabulary ~45029–45092, spellcheck IO
  ~52262–52280, llm/proxy ~56987–57111, api keys ~61235–61247, «недавние
  проекты» ~32231–32430.
Отдельно: между этими координатами есть необъяснённые разрывы (например
~44619–44638, ~44930 vs 44931, ~45092–45228) — проверить, что там (может
быть settings-код, который координатор пропустил грепом по имени).
Плюс — полный скан класса на предмет ЛЮБЫХ других методов с
settings/config/IO-паттерном в имени или теле (чтение/запись json,
Path/os.path на user_data), которые НЕ попали в список выше — предвари-
тельный грep не исчерпывающий.

1.2 ТОЧНЫЙ ИТОГОВЫЙ СОСТАВ
Окончательный подтверждённый список методов на перенос в
modules/settings_service.py. Сравнить с ожиданием плана "~25" и с
предварительной оценкой координатора "~46" — объяснить расхождение.
Явно подтвердить точные границы обеих функций миграции.

1.3 ПРОВЕРКА "CIRCULAR DEPENDENCY RISKS: НЕТ" (план утверждает это, не
    принимать на веру)
Для каждого метода из 1.2 — вызывает ли он что-то из UI/grid-слоя (уже
вынесенного в Step 5/6 в modules/), что могло бы создать цикл при
переносе в settings_service.py? Явно подтвердить или опровергнуть
утверждение плана фактами.

1.4 CALL SITES — С ОСОБЫМ ФОКУСОМ НА load_general_settings
Repo-wide grep/AST по всем ~46 методам. Для load_general_settings —
ТОЧНОЕ число реальных вызовов (не текстовых совпадений), с разбивкой:
внутри переносимых методов / из методов, остающихся в монолите / внешние
(modules/*.py) / из UI-обработчиков. Для каждого внешнего или out-of-
monolith вызова — подтвердить, что он не полагается на кэширование
результата между вызовами (т.е. ожидает свежего чтения с диска каждый
раз) — это прямая проверка семантики, которую нельзя сломать.
Аналогично (короче) для остальных методов состава.

1.5 ЗАВИСИМОСТИ / ОТНОШЕНИЕ К СУЩЕСТВУЮЩЕМУ ConfigManager
Таблица: метод → какие self.-атрибуты использует (user_data_dir, пути,
и т.п.) → есть ли уже доступ к экземпляру ConfigManager через self
(например self.config_manager) или пути вычисляются заново на месте?
Явно определить, ДУБЛИРУЕТ ли что-то из состава уже существующую логику
ConfigManager (user_data paths) или это ортогонально (разные форматы
данных — general settings JSON vs user_data-location config).

1.6 DYNAMIC_ENTRY_POINT / СТРОКОВЫЕ ССЫЛКИ
Проверить весь список 1.2 на DYNAMIC_ENTRY_POINT в DEAD_CODE_REPORT.md и
на паттерн из Step 6 (строковые action-таблицы вроде
modules/shortcut_manager.py) — если найдутся, это определяет
необходимость точных делегатов-обёрток.

1.7 РЕКОМЕНДАЦИЯ ПО АРХИТЕКТУРЕ (не решение агента)
На основе 1.3/1.5 — предложить: (а) расширить класс ConfigManager
напрямую новыми методами, (б) новый класс SettingsService, оборачивающий
экземпляр ConfigManager как зависимость, (в) функции с явными параметрами
(путь/config_manager передаются аргументом), использующие ConfigManager
только для путей — по аналогии с modules/grid/, но здесь дополнительно
есть готовый класс, который стоит переиспользовать, а не игнорировать.
Дать обоснование с опорой на факты 1.5, не копировать решение Step 5/6 по
умолчанию.

1.8 РЕКОМЕНДАЦИЯ ПО РАЗБИВКЕ STAGE 2+ НА ПОД-БАТЧИ
~46 методов одним коммитом — неприемлемо по размеру/риску (сравни:
filters.py в Step 6 было 20 методов и уже потребовало трёх моделей на
Этапе 3). Предложить разбивку — вероятно по логическим кластерам: ядро
(unified settings load/save + миграции + general-from-file, самый
высокий риск из-за fan-in) отдельно от периферии (spellcheck IO,
llm/proxy, api keys, «недавние проекты» — каждый маленький и
физически далеко от остальных). Дать конкретный порядок и обоснование,
с оценкой числа методов/call sites на под-батч. Если состав какого-то
под-батча выйдет ≥10 методов — отметить явно (для конвенции живого
чек-листа Этапа 2, см. AGENTS.md/ways-of-working).

1.9 СЕМАНТИКА "ЧТЕНИЕ С ДИСКА ПРИ КАЖДОМ ВЫЗОВЕ" — ЗАФИКСИРОВАТЬ, НЕ
    ЧИНИТЬ
Явно задокументировать в отчёте: load_general_settings (и любые другие
методы состава с похожим паттерном) не кэшируют результат между
вызовами. Отметить как ОБЯЗАТЕЛЬНОЕ требование для Stage 2+: чистая
функция должна воспроизводить это поведение (открывать файл заново
каждый раз), даже если это выглядит как "лишний I/O" — это не повод
оптимизировать на этапе механического переноса.

═══════════════════════════════════════════════════════════════
СТОП-УСЛОВИЕ STAGE 1
═══════════════════════════════════════════════════════════════
После завершения 1.1–1.9 — ОСТАНОВИТЬСЯ. Не создавать
modules/settings_service.py, не трогать Supervertaler.py и
modules/config_manager.py. Собрать финальный отчёт (формат ниже),
записать его в docs/refactoring/reports/ОТЧЁТ Batch #7 Stage 1.txt и
ждать подтверждения владельца по архитектуре, итоговому составу и
разбивке на под-батчи.

═══════════════════════════════════════════════════════════════
ФОРМАТ ОТЧЁТА STAGE 1
═══════════════════════════════════════════════════════════════
- Полная таблица 1.1/1.2 (метод → границы → кластер → комментарий),
  включая объяснение необъяснённых разрывов из 1.1.
- Итоговое число vs ожидания плана "~25" и координатора "~46" — с
  объяснением расхождения.
- Результат проверки 1.3 (circular dependency) — подтверждено/опровергнуто,
  с фактами.
- Полная разбивка call sites (1.4) с точным числом для
  load_general_settings и подтверждением "не кэшируется нигде" по всем
  внешним вызовам.
- Таблица зависимостей и отношения к ConfigManager (1.5).
- Результат проверки DYNAMIC_ENTRY_POINT (1.6).
- Рекомендация по архитектуре (1.7) с обоснованием.
- Рекомендация по разбивке Stage 2+ (1.8) — конкретный порядок,
  обоснование, оценка размера каждого под-батча, отметка где состав ≥10
  методов (для живого чек-листа Этапа 2).
- Явное подтверждение семантики "чтение с диска" (1.9).
- Явный вывод: "ГОТОВ К STAGE 2" или "ЕСТЬ БЛОКЕРЫ/ОТКРЫТЫЕ ВОПРОСЫ".
- Подтверждение: ни одна строка кода не изменена.

Обновляй docs/refactoring/audits/step7_stage1_findings.md по ходу (после
каждой подзадачи), не только в конце — см. блок про передачу смены в
начале этого промпта.
