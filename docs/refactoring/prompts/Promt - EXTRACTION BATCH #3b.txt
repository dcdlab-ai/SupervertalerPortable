РОЛЬ: Ты выполняешь механический, атомарный перенос кода в проекте Supervertaler.
Режим: ACTIVE REFACTOR, но с обязательными pre-flight проверками (как в
Batch #1, #2 и #3a). Если любая pre-flight проверка провалится — ОСТАНОВИСЬ
и сообщи о несоответствии, не пытайся "исправить попутно".

ПРЕДПОСЫЛКА: этот батч запускается только после подтверждённого GO по
Batch #3a (event-фильтры → modules/event_filters.py).

СКОУП BATCH #3b (и ничего больше):
Перенести 6 классов диалогов из Supervertaler.py (диапазоны строк
7190–7786 и 8782–9091) в НОВЫЙ пакет modules/dialogs/:
ThemeEditorDialog, DetachedLogWindow, AdvancedFiltersDialog, ScratchpadDialog,
LiveProgressDialog, _ImportProgressDialog.
Чекбоксы/слияние со styled_widgets.py (Batch #3c) — НЕ в этом батче.

ВАЖНАЯ ОСОБЕННОСТЬ СКОУПА: LiveProgressDialog и _ImportProgressDialog
физически расположены внутри более широкого диапазона 7787–9169, который
CODE_MAP_REFACTOR.md §3 маркирует как "QThread-воркеры" — по соседству с
TMSearchWorker, PreTranslationWorker, ProofreadWorker, GlossaryExtractionWorker.
Эти 4 воркера НЕ входят в скоуп этого батча (отдельный Step 4 плана,
будущий Batch #4). На этапе 1.1 нужно точно вырезать границы именно двух
диалогов, не задев тела соседних воркеров ни на строку.

ЖЁСТКИЕ ЗАПРЕТЫ:
- Не переименовывать классы, не менять сигнатуры конструкторов.
- Не трогать тела TMSearchWorker, PreTranslationWorker, ProofreadWorker,
  GlossaryExtractionWorker — они физически рядом, но не в скоупе.
- Не трогать DYNAMIC_ENTRY_POINT методы.
- Не удалять и не помечать dead-код.
- Не создавать modules/dialogs.py файлом — именно пакет modules/dialogs/
  с раздельными файлами, как указано в EXTRACTION_PLAN.md Step 3.
- Один шаг = один коммит.

═══════════════════════════════════════════════════════════════
ЭТАП 0 — БАЗОВАЯ ЛИНИЯ
═══════════════════════════════════════════════════════════════
1. Убедиться, что рабочая копия чистая (git status), продолжить от коммита
   Batch #3a.
2. SHA256 всех .py файлов → manifest_before_batch3b.txt.
3. py_compile всех .py файлов — чисто.
4. Подсчитать baseline_counts_batch3b.txt: те же 8 метрик — .connect(,
   QShortcut(, create_shortcut(, QTimer(, timeout.connect, QTimer.timeout,
   singleShot, QMetaObject.invokeMethod.

═══════════════════════════════════════════════════════════════
ЭТАП 1 — PRE-FLIGHT (не начинать перенос, пока весь этап не пройден)
═══════════════════════════════════════════════════════════════

1.1 ГРАНИЦЫ И СОСТАВ (AST, не доверять дословно числам из отчётов)
- Найти реальные границы каждого из 6 диалогов через AST. Отправная точка
  (CODE_MAP_REFACTOR.md §13.2, ПРОВЕРИТЬ, не принимать на веру):
  ThemeEditorDialog (~7190), DetachedLogWindow (~7407),
  AdvancedFiltersDialog (~7491), ScratchpadDialog (~7715),
  LiveProgressDialog (~8782), _ImportProgressDialog (~8958).
- Отдельно и явно зафиксировать конец тела ScratchpadDialog и начало
  следующего класса (первого воркера) — граница 7786/7787 должна быть
  чистой, без обрезанных строк.
- Отдельно и явно зафиксировать, что вырезка LiveProgressDialog и
  _ImportProgressDialog не задевает код ProofreadWorker/PreTranslationWorker,
  которые (по CODE_MAP_REFACTOR.md §3) расположены в том же общем диапазоне
  7787–9169.
- Если фактический список/границы отличаются от заявленного — сообщить и
  уточнить скоуп ПЕРЕД переносом.

1.2 ЧИСТОТА КЛАССОВ / ИНТЕРФЕЙС
- Для каждого из 6 диалогов: какие параметры принимает __init__
  (parent/аргументы данных)? EXTRACTION_PLAN.md утверждает, что ни один не
  имеет прямой зависимости от MainWindow — проверить это фактически по телу
  каждого класса, а не поверить утверждению.
- Если какой-то диалог обращается к self/parent сверх заявленного —
  зафиксировать явно, это меняет способ переноса.

1.3 СОДЕРЖИМОЕ modules/dialogs/ — ПОЛНАЯ ИНВЕНТАРИЗАЦИЯ
- Подтвердить, что пакета/файла modules/dialogs/ (или dialogs.py) не
  существует. Если существует что-то похожее — прочитать целиком прежде
  чем добавлять.

1.4 КРИТИЧЕСКИЙ ГЕЙТ — ПОВЕДЕНЧЕСКОЕ СРАВНЕНИЕ "ДУБЛЕЙ"
Обязательный шаг для каждого из 6 диалогов отдельно, даже если ожидается
пустой результат.
- Grep по всему modules/ на класс с тем же именем или тем же назначением
  (например, другой прогресс-диалог, другой лог-вьюер) для каждого из 6.
- Если что-то похожее найдено — сравнить поведение на 3-5 сценариях
  (открыть/применить/отменить/закрыть), не считать "одинаковым" по
  названию.
- Если найдено расхождение — ОСТАНОВИТЬСЯ, показать конкретный пример,
  ждать решения владельца.
- Если ничего похожего не найдено — зафиксировать явно по каждому из 6
  диалогов отдельно: "дублей не найдено, гейт пройден".

1.5 CALLSITES И CALLERS
- Grep по всему репозиторию на конструктор каждого из 6 классов
  (ClassName(...)). Полный список → callsites_before_batch3b.txt.
- Отдельно определить точные call site'ы LiveProgressDialog и
  _ImportProgressDialog — какие именно методы монолита их показывают
  (ожидаемо что-то из области импорта, например _import_multifile_project
  или import_folder_multifile — подтвердить фактически, не предполагать).
- Эти 6 классов — top-level классы, не self-методы SupervertalerQt.
  Решение по умолчанию: делегаты не требуются. modules/dialogs/__init__.py
  реэкспортирует все 6 имён, монолит импортирует их оттуда одной строкой,
  call sites конструкторов не меняются текстуально.

1.6 ПРОВЕРКА НА DYNAMIC_ENTRY_POINT И СТРОКОВЫЕ ССЫЛКИ
- Проверить каждое из 6 имён на getattr/hasattr/setattr,
  QMetaObject.invokeMethod, DYNAMIC_ENTRY_POINT список.

1.7 ЛЕНИВЫЕ ИМПОРТЫ ВНУТРИ КЛАССОВ
- Проверить локальные import-выражения внутри тел всех 6 диалогов,
  перенести как есть вместе с телом.

═══════════════════════════════════════════════════════════════
ЭТАП 2 — ПЕРЕНОС (только после чистого прохождения Этапа 1.4)
═══════════════════════════════════════════════════════════════
1. Создать пакет modules/dialogs/ с файлами: theme_editor.py,
   detached_log.py, advanced_filters.py, scratchpad.py, live_progress.py,
   import_progress.py.
2. Перенести каждый диалог ВЕРБАТИМ в свой файл (один класс — один файл,
   как перечислено выше).
3. Создать modules/dialogs/__init__.py, реэкспортирующий все 6 классов по
   имени (from .theme_editor import ThemeEditorDialog, и т.д.), чтобы
   `from modules.dialogs import ThemeEditorDialog` работало.
4. В Supervertaler.py добавить импорт:
   from modules.dialogs import (ThemeEditorDialog, DetachedLogWindow,
   AdvancedFiltersDialog, ScratchpadDialog, LiveProgressDialog,
   _ImportProgressDialog)
5. Удалить оригинальные определения из Supervertaler.py, не трогая соседние
   классы-воркеры (проверить диффом, что их тела не изменились ни на строку).

═══════════════════════════════════════════════════════════════
ЭТАП 3 — ВАЛИДАЦИЯ (обязательна, отчитаться по каждому пункту)
═══════════════════════════════════════════════════════════════
1. python -m py_compile на Supervertaler.py и всех новых файлах
   modules/dialogs/ — чисто.
2. Повторить подсчёт 8 метрик — сравнить с baseline_counts_batch3b.txt,
   точное совпадение.
3. Повторить grep callsites — сравнить с callsites_before_batch3b.txt,
   идентичны (только строка импорта новая).
4. SHA256 всех файлов, кроме Supervertaler.py и 7 новых файлов
   modules/dialogs/ (6 + __init__.py) — должны совпасть с
   manifest_before_batch3b.txt. ОТДЕЛЬНО подтвердить, что тела 4-х
   воркеров, физически соседствовавших в файле, дают нулевой diff.
5. ФУНКЦИОНАЛЬНЫЕ СЦЕНАРИИ (прогнать все, по каждому диалогу отдельно,
   пункт за пунктом):
   - ThemeEditorDialog: открыть редактор темы из меню, сменить цвет
     минимум одного элемента, Apply, убедиться что refresh_theme_colors
     реально перекрашивает грид/панели.
   - DetachedLogWindow: отсоединить лог-окно, убедиться что новые строки
     лога продолжают туда попадать, закрыть/переприсоединить.
   - AdvancedFiltersDialog: применить фильтр по нескольким условиям, Reset,
     Cancel — грид отражает изменения корректно.
   - ScratchpadDialog: ввести текст, переключить вкладку, вернуться — текст
     сохранён; закрыть и переоткрыть проект — текст восстановлен.
   - LiveProgressDialog: запустить операцию, которая его показывает (импорт
     папки/мультифайл), убедиться что прогресс обновляется и UI не
     подвисает.
   - _ImportProgressDialog: то же для конкретного импорта, где он
     фактически используется (определено в 1.5).
6. Обычный smoke-тест (как в предыдущих батчах).

═══════════════════════════════════════════════════════════════
ЭТАП 4 — ОТЧЁТ (обязательный формат)
═══════════════════════════════════════════════════════════════
- Результат Этапа 1.1: подтверждение точных границ всех 6 диалогов и
  подтверждение, что соседние 4 воркера не задеты (с номерами строк).
- Результат Этапа 1.4 по каждому из 6 диалогов отдельно — "дублей не
  найдено" либо результат сравнения с примерами.
- Diff stat (git diff --stat).
- Callsites diff с объяснением.
- Counts diff — все 8 метрик.
- SHA256 diff — какие файлы изменились (ожидается 1 + 7 = 8: Supervertaler.py
  плюс 6 файлов диалогов плюс __init__.py), явное подтверждение нулевого
  диффа тел соседних воркеров.
- Чеклист функциональных сценариев (Этап 3.5) — по каждому диалогу отдельно,
  пройдено/не пройдено.
- Результат обычного smoke-теста.
- Явный GO/NO-GO с обоснованием.
- Один git-коммит: "Extract dialogs into modules/dialogs/ package
  (Batch #3b, Step 3 of EXTRACTION_PLAN.md)".

СТОП-УСЛОВИЯ:
1. Если Этап 1.4 находит расхождение поведения для любого из 6 диалогов —
   остановиться немедленно, показать конкретный пример, ждать решения
   владельца.
2. Если границы диалогов (1.1) налезают на тело соседних воркеров хотя бы
   на одну строку — остановиться, не резать "на глаз", уточнить точную
   границу.
3. Если фактический AST-состав (1.1) отличается от заявленного скоупа —
   остановиться, уточнить скоуп.
4. После Этапа 4 — остановиться и ждать подтверждения человека перед тем,
   как переходить к Batch #3c (чекбоксы). Не продолжать автоматически.
